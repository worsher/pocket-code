{
  "name": "@pocket-code/relay",
  "version": "0.1.0",
  "description": "Device pairing + message relay + HTTP/WS reverse tunnel server (ngrok-style, single-tenant shared-secret)",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/worsher/pocket-code.git",
    "directory": "packages/relay"
  },
  "keywords": [
    "reverse-tunnel",
    "relay",
    "ngrok-alternative",
    "websocket"
  ],
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run src"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "ws": "^8.18.0",
    "@pocket-code/protocol-core": "workspace:*"
  },
  "devDependencies": {
    "@pocket-code/tunnel-client": "workspace:*",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
