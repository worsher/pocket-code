{
  "name": "@pocket-code/protocol-core",
  "version": "0.1.0",
  "description": "Business-agnostic relay protocol schemas: envelopes, pairing/discovery, tunnel frames, boundary unions (zod)",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/worsher/pocket-code.git",
    "directory": "packages/protocol-core"
  },
  "keywords": [
    "reverse-tunnel",
    "relay",
    "ngrok-alternative",
    "websocket"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "test": "vitest run src" },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
