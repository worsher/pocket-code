# 双模式隧道路由(子域默认 + 路径前缀可切)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让隧道预览支持两种寻址模式——默认子域模式(`<machineId>-<port>.tunnel.<domain>`,完整解法:绝对路径天然正确、链接可分享、SPA 不掉前缀),`TUNNEL_MODE=path` 回退路径前缀模式并补 `Location` 前缀。

**Architecture:** relay 入口把"从请求解析 machineId+port"抽成单一纯函数 `resolveTunnelTarget`(两个 router 共用,消除重复);路径模式下用纯函数 `rewriteRedirectHeaders` 在 `TunnelHub.onResponse` 出口补 `Location` 前缀(收编 daemon fetch 吞 3xx 的偏差);daemon fetch 改 `redirect:"manual"` 让 3xx 透传;machineId 改纯 hex 使其 DNS-safe;`/health` 暴露模式供 App 拼 URL。

**Tech Stack:** TypeScript(ESM,NodeNext),vitest,ws,undici fetch,React Native(App PreviewTab)。

## Global Constraints

以下每条都是每个任务隐含的需求,值一字不差照抄自 spec:

- **默认模式 = `subdomain`**;`TUNNEL_MODE=path` 显式切换。判定沿用 `isDiscoveryEnabled` 风格(默认值 + 仅特定字符串切换,`.trim().toLowerCase()`,大小写/空格不敏感)。
- **machineId 改为纯 16 位 hex**(去掉 `m_` 前缀)。两处生成点必须同时改:`packages/daemon/src/index.ts` 与 `packages/tunnel-client/src/identity.ts`。
- 子域模式下 `TUNNEL_BASE_DOMAIN` **必需**;缺失则启动**快速失败**(抛错带指引 `TUNNEL_BASE_DOMAIN required when TUNNEL_MODE=subdomain (e.g. tunnel.example.com)`,`process.exit(1)`),不静默降级。
- **鉴权语义两模式一致**:`TUNNEL_TOKEN` 判定、404 uniform、`pc_token` 剥离、`TUNNEL_COOKIE_SECURE` 开关全部与寻址模式正交、两模式共用。
- **路径模式补前缀只作用于 HTTP `Location` 头**;**不改写任何 Set-Cookie**(含 relay 自己的 `pc_tunnel`/`pc_tunnel_token` 与应用自身 cookie)。`pc_tunnel` 保持 `Path=/`。
- HTTP 与 WS-upgrade 两个入口必须共享同一寻址语义 `resolveTunnelTarget`(消除现有两份漂移的正则/cookie 逻辑)。
- TDD:每个纯函数模块 RED→GREEN。测试不得断言空。
- 许可证 MIT(沿用 relay 包,无需改动)。

## 文件结构

- **新增** `packages/relay/src/tunnelAddress.ts` — 唯一寻址决策纯函数 `resolveTunnelTarget`(+ 类型 `TunnelMode`/`TunnelTarget`/`ResolveResult`)。两 router 共用。
- **新增** `packages/relay/src/tunnelAddress.test.ts`
- **新增** `packages/relay/src/redirectRewrite.ts` — 纯函数 `rewriteRedirectHeaders`(仅路径模式补 `Location`)。
- **新增** `packages/relay/src/redirectRewrite.test.ts`
- **新增** `packages/app/src/components/PreviewTab/tunnelUrl.ts` — 从 `index.tsx` 抽出的可测 URL 逻辑(`buildTunnelUrl` / `maybeRewriteToTunnel` / 类型 `TunnelInfo`)。
- **新增** `packages/app/src/components/PreviewTab/tunnelUrl.test.ts`
- **改** `packages/relay/src/config.ts` — 加 `TunnelMode` 类型、`getTunnelMode`、`getTunnelBaseDomain`。
- **改** `packages/relay/src/config.test.ts` — 加对应测试。
- **改** `packages/tunnel-client/src/tunnel.ts` — `proxyToLocalhost` 的 `init` 加 `redirect:"manual"`。
- **改** `packages/tunnel-client/src/tunnel.test.ts` — 加 307 透传测试。
- **改** `packages/relay/src/tunnelHub.ts` — `PendingTunnel` 加可选 `rewriteHeaders` 回调,`onResponse` 应用它。
- **改** `packages/relay/src/httpRouter.ts` — deps 加 `tunnelMode`/`tunnelBaseDomain`;寻址换成 `resolveTunnelTarget`;`/health` 加两字段;路径模式 open 时传入 redirectRewrite 回调。
- **改** `packages/relay/src/httpRouter.test.ts` — 加两模式测试。
- **改** `packages/relay/src/upgradeRouter.ts` — deps 加 `tunnelMode`/`tunnelBaseDomain`;寻址换成 `resolveTunnelTarget`。
- **改** `packages/relay/src/upgradeRouter.test.ts`(若存在;不存在则本任务新增该文件的相关用例)
- **改** `packages/relay/src/index.ts` — 读 mode/baseDomain,快速失败,注入两 router 的 deps。
- **改** `packages/daemon/src/index.ts:57` + `packages/tunnel-client/src/identity.ts:30` — machineId 纯 hex。
- **改** `packages/app/src/components/PreviewTab/index.tsx` — 改为 import `tunnelUrl.ts`;组件内拉 `/health` 缓存 tunnelInfo。
- **改** `packages/app/src/store/settings.ts` — 加两个可选运行时字段 `relayTunnelMode?`/`relayTunnelBaseDomain?`。
- **改** `packages/relay/src/e2e.tunnel.test.ts` — 加子域 scenario。
- **改** `packages/relay/README.md` + `docs/deployment-relay-daemon.md`(若存在)— 文档两模式 + Cloudflare DNS-01 + 破坏性变更迁移。

---

### Task 1: machineId 改纯 hex(两处生成点)

**Files:**
- Modify: `packages/daemon/src/index.ts:57`
- Modify: `packages/tunnel-client/src/identity.ts:30`
- Test: `packages/tunnel-client/src/identity.test.ts`(若不存在则创建)

**Interfaces:**
- Consumes: 无
- Produces: machineId 格式 = 16 位小写 hex(如 `bda488cfdee21ccc`),无 `m_` 前缀。后续任务的 `resolveTunnelTarget` 子域正则依赖此格式 DNS-safe。

**背景:** machineId 当前是 `m_<8字节hex>`,下划线在主机名里非法。改为纯 hex 使子域 `<machineId>-<port>.tunnel.<domain>` 合法。前缀不承载信息(唯一性靠随机 hex)。

- [ ] **Step 1: 写失败测试(identity.ts 纯 hex)**

先确认 `packages/tunnel-client/src/identity.test.ts` 是否存在。若不存在,创建:

```ts
import { describe, it, expect, afterEach } from "vitest";
import { loadOrCreateIdentity } from "./identity.js";
import { rmSync, existsSync } from "fs";
import { join } from "path";
import { tmpdir } from "os";

const TMP = join(tmpdir(), `pc-identity-test-${process.pid}.json`);
afterEach(() => { if (existsSync(TMP)) rmSync(TMP); });

describe("loadOrCreateIdentity", () => {
  it("生成的 machineId 是纯 16 位 hex,无 m_ 前缀(DNS-safe)", () => {
    const id = loadOrCreateIdentity(TMP, "test-host").machineId;
    expect(id).toMatch(/^[0-9a-f]{16}$/);
    expect(id.startsWith("m_")).toBe(false);
  });
  it("已存在文件时读回原值不重生成", () => {
    const first = loadOrCreateIdentity(TMP, "h").machineId;
    const second = loadOrCreateIdentity(TMP, "h").machineId;
    expect(second).toBe(first);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @pocket-code/tunnel-client exec vitest run src/identity.test.ts`
Expected: FAIL — 现值形如 `m_xxxx`,`toMatch(/^[0-9a-f]{16}$/)` 不过。

- [ ] **Step 3: 改 identity.ts:30**

`packages/tunnel-client/src/identity.ts` 第 30 行:

```ts
    machineId: crypto.randomBytes(8).toString("hex"),
```

（删掉 `` m_${...} `` 模板,直接用 hex。）

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @pocket-code/tunnel-client exec vitest run src/identity.test.ts`
Expected: PASS

- [ ] **Step 5: 改 daemon index.ts:57**

`packages/daemon/src/index.ts` 第 57 行(`loadOrGenerateMachineId` 内):

```ts
    const id = crypto.randomBytes(8).toString("hex");
```

- [ ] **Step 6: daemon 构建通过(无独立测试覆盖此行,靠 typecheck/build)**

Run: `pnpm --filter @pocket-code/daemon build`
Expected: 构建成功,无 TS 错误。

- [ ] **Step 7: Commit**

```bash
git add packages/tunnel-client/src/identity.ts packages/tunnel-client/src/identity.test.ts packages/daemon/src/index.ts
git commit -m "feat(tunnel): machineId 改纯 16 位 hex(DNS-safe,去 m_ 前缀)"
```

---

### Task 2: config 加 getTunnelMode / getTunnelBaseDomain

**Files:**
- Modify: `packages/relay/src/config.ts`
- Test: `packages/relay/src/config.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `export type TunnelMode = "subdomain" | "path";`
  - `export function getTunnelMode(env?: Record<string, string | undefined>): TunnelMode;` — 默认 `"subdomain"`,仅当 env 值 `.trim().toLowerCase() === "path"` 返回 `"path"`。
  - `export function getTunnelBaseDomain(env?: Record<string, string | undefined>): string | null;` — `(env.TUNNEL_BASE_DOMAIN||"").trim()` 非空则返回,否则 `null`。

- [ ] **Step 1: 写失败测试**

在 `packages/relay/src/config.test.ts` 末尾追加(并在顶部 import 里加 `getTunnelMode, getTunnelBaseDomain, type TunnelMode`):

```ts
describe("getTunnelMode", () => {
  it("默认 subdomain(未设置/空/其他值)", () => {
    expect(getTunnelMode({})).toBe("subdomain");
    expect(getTunnelMode({ TUNNEL_MODE: "" })).toBe("subdomain");
    expect(getTunnelMode({ TUNNEL_MODE: "weird" })).toBe("subdomain");
  });
  it("仅 'path' 切换(大小写/空格不敏感)", () => {
    expect(getTunnelMode({ TUNNEL_MODE: "path" })).toBe("path");
    expect(getTunnelMode({ TUNNEL_MODE: " PATH " })).toBe("path");
  });
});

describe("getTunnelBaseDomain", () => {
  it("trim 后非空返回,空/未设置返回 null", () => {
    expect(getTunnelBaseDomain({})).toBeNull();
    expect(getTunnelBaseDomain({ TUNNEL_BASE_DOMAIN: "  " })).toBeNull();
    expect(getTunnelBaseDomain({ TUNNEL_BASE_DOMAIN: " tunnel.aigc.zj.cn " })).toBe("tunnel.aigc.zj.cn");
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/config.test.ts`
Expected: FAIL — `getTunnelMode is not a function` / 导入不存在。

- [ ] **Step 3: 实现**

在 `packages/relay/src/config.ts` 末尾追加:

```ts
/** 隧道寻址模式:子域(默认)或路径前缀。 */
export type TunnelMode = "subdomain" | "path";

/** TUNNEL_MODE:默认 subdomain,仅显式 "path" 切换(同 isDiscoveryEnabled 风格)。 */
export function getTunnelMode(
  env: Record<string, string | undefined> = process.env
): TunnelMode {
  return (env.TUNNEL_MODE || "").trim().toLowerCase() === "path" ? "path" : "subdomain";
}

/** TUNNEL_BASE_DOMAIN:子域模式必需(如 "tunnel.aigc.zj.cn");trim 后空 → null。 */
export function getTunnelBaseDomain(
  env: Record<string, string | undefined> = process.env
): string | null {
  const d = (env.TUNNEL_BASE_DOMAIN || "").trim();
  return d || null;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/config.test.ts`
Expected: PASS(含原有全部 config 测试)。

- [ ] **Step 5: Commit**

```bash
git add packages/relay/src/config.ts packages/relay/src/config.test.ts
git commit -m "feat(relay): config 加 getTunnelMode/getTunnelBaseDomain(默认 subdomain)"
```

---

### Task 3: tunnelAddress.ts —— 唯一寻址纯函数

**Files:**
- Create: `packages/relay/src/tunnelAddress.ts`
- Test: `packages/relay/src/tunnelAddress.test.ts`

**Interfaces:**
- Consumes: `TunnelMode`(from config.ts,Task 2)
- Produces:
  ```ts
  export interface TunnelTarget { machineId: string; port: number; forwardPath: string; }
  export type ResolveResult =
    | { kind: "tunnel"; target: TunnelTarget }
    | { kind: "control" }
    | { kind: "none" };
  export function resolveTunnelTarget(
    mode: TunnelMode,
    host: string | undefined,
    pathname: string,
    cookieHeader: string | undefined,
    baseDomain: string | null
  ): ResolveResult;
  ```
  `forwardPath` **不含** query,不含 `pc_token` 处理——调用方负责 query 拼接与 `pc_token` 剥离。子域模式 `forwardPath = pathname`;路径模式 = 剥前缀后的 rest(默认 `/`)。

**背景:** 见 spec「文件结构 › tunnelAddress.ts」。这是两个 router 共用的寻址决策点,消除现有内联正则/cookie 的两份漂移。

- [ ] **Step 1: 写失败测试**

创建 `packages/relay/src/tunnelAddress.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { resolveTunnelTarget } from "./tunnelAddress.js";

const BASE = "tunnel.aigc.zj.cn";

describe("resolveTunnelTarget — subdomain 模式", () => {
  it("合法子域 Host 解析出 machineId+port,forwardPath=pathname", () => {
    const r = resolveTunnelTarget("subdomain", `bda488cfdee21ccc-3000.${BASE}`, "/admin", undefined, BASE);
    expect(r).toEqual({ kind: "tunnel", target: { machineId: "bda488cfdee21ccc", port: 3000, forwardPath: "/admin" } });
  });
  it("Host 带端口后缀(:443)仍能解析(浏览器可能带)", () => {
    const r = resolveTunnelTarget("subdomain", `bda488cfdee21ccc-3000.${BASE}:443`, "/", undefined, BASE);
    expect(r.kind).toBe("tunnel");
    if (r.kind === "tunnel") expect(r.target).toMatchObject({ machineId: "bda488cfdee21ccc", port: 3000 });
  });
  it("baseDomain 本身 → control(非隧道)", () => {
    expect(resolveTunnelTarget("subdomain", BASE, "/relay", undefined, BASE)).toEqual({ kind: "control" });
  });
  it("主站域 / 不匹配 → control", () => {
    expect(resolveTunnelTarget("subdomain", "aigc.zj.cn", "/", undefined, BASE)).toEqual({ kind: "control" });
  });
  it("裸 IP Host → control", () => {
    expect(resolveTunnelTarget("subdomain", "139.196.157.166", "/", undefined, BASE)).toEqual({ kind: "control" });
  });
  it("端口段非数字 → control(不是合法隧道子域)", () => {
    expect(resolveTunnelTarget("subdomain", `bda488cfdee21ccc-abc.${BASE}`, "/", undefined, BASE)).toEqual({ kind: "control" });
  });
  it("baseDomain 为 null(配置缺失)→ control(不 crash)", () => {
    expect(resolveTunnelTarget("subdomain", `x-3000.${BASE}`, "/", undefined, null)).toEqual({ kind: "control" });
  });
  it("Host 缺失 → control", () => {
    expect(resolveTunnelTarget("subdomain", undefined, "/", undefined, BASE)).toEqual({ kind: "control" });
  });
  it("忽略 /t/ 前缀(子域模式不认路径寻址)", () => {
    const r = resolveTunnelTarget("subdomain", `aa11bb22cc33dd44-8080.${BASE}`, "/t/other/9999/x", undefined, BASE);
    expect(r.kind).toBe("tunnel");
    if (r.kind === "tunnel") expect(r.target).toMatchObject({ port: 8080, forwardPath: "/t/other/9999/x" });
  });
});

describe("resolveTunnelTarget — path 模式", () => {
  it("/t/id/port/rest 解析,forwardPath=rest", () => {
    const r = resolveTunnelTarget("path", "aigc.zj.cn", "/t/bda488cfdee21ccc/3000/admin", undefined, null);
    expect(r).toEqual({ kind: "tunnel", target: { machineId: "bda488cfdee21ccc", port: 3000, forwardPath: "/admin" } });
  });
  it("/t/id/port(无尾 rest)→ forwardPath '/'", () => {
    const r = resolveTunnelTarget("path", "h", "/t/abc123/5173", undefined, null);
    expect(r.kind).toBe("tunnel");
    if (r.kind === "tunnel") expect(r.target.forwardPath).toBe("/");
  });
  it("pc_tunnel cookie 兜底,forwardPath=pathname", () => {
    const r = resolveTunnelTarget("path", "h", "/_next/x.js", "pc_tunnel=abc123:5173", null);
    expect(r).toEqual({ kind: "tunnel", target: { machineId: "abc123", port: 5173, forwardPath: "/_next/x.js" } });
  });
  it("/relay 与 /relay/ → control", () => {
    expect(resolveTunnelTarget("path", "h", "/relay", undefined, null)).toEqual({ kind: "control" });
    expect(resolveTunnelTarget("path", "h", "/relay/", undefined, null)).toEqual({ kind: "control" });
  });
  it("无 /t/ 无 cookie → none", () => {
    expect(resolveTunnelTarget("path", "h", "/whatever", undefined, null)).toEqual({ kind: "none" });
  });
  it("忽略 Host 子域(路径模式不认 Host 寻址)", () => {
    const r = resolveTunnelTarget("path", `zzz-1234.${BASE}`, "/t/abc123/3000/", undefined, BASE);
    expect(r.kind).toBe("tunnel");
    if (r.kind === "tunnel") expect(r.target).toMatchObject({ machineId: "abc123", port: 3000 });
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/tunnelAddress.test.ts`
Expected: FAIL — 模块不存在。

- [ ] **Step 3: 实现**

创建 `packages/relay/src/tunnelAddress.ts`:

```ts
// ── 隧道寻址:从请求解析出目标 machineId+port(两 router 唯一决策点) ──
// 子域模式从 Host 解析 <machineId>-<port>.<baseDomain>;路径模式从 /t/ 前缀
// 或 pc_tunnel cookie 解析。纯函数:不做 pc_token 剥离/query 拼接/鉴权(调用方负责)。

import type { TunnelMode } from "./config.js";

export interface TunnelTarget {
  machineId: string;
  port: number;
  /** 转发给 daemon 的 path(不含 query)。子域=原样 pathname;路径=剥前缀后的 rest。 */
  forwardPath: string;
}

export type ResolveResult =
  | { kind: "tunnel"; target: TunnelTarget }
  | { kind: "control" } // 非隧道请求(控制通道 /relay,或子域模式的主站/非匹配 Host)
  | { kind: "none" };   // 既非隧道也非控制(HTTP 该 404)

const PATH_TUNNEL_RE = /^\/t\/([^/]+)\/(\d+)(\/.*)?$/;
const PATH_COOKIE_RE = /(?:^|;\s*)pc_tunnel=([^:;]+):(\d+)/;
// machineId(hex)-port,后接 .<baseDomain>。machineId 段限 hex 以贴合纯 hex 生成规则。
const SUB_LABEL_RE = /^([0-9a-f]+)-(\d+)$/i;

export function resolveTunnelTarget(
  mode: TunnelMode,
  host: string | undefined,
  pathname: string,
  cookieHeader: string | undefined,
  baseDomain: string | null
): ResolveResult {
  if (mode === "subdomain") {
    if (!host || !baseDomain) return { kind: "control" };
    const hostname = host.split(":")[0]; // 去掉可能的 :port
    const suffix = `.${baseDomain}`;
    if (!hostname.endsWith(suffix)) return { kind: "control" };
    const label = hostname.slice(0, -suffix.length); // <machineId>-<port>
    const m = label.match(SUB_LABEL_RE);
    if (!m) return { kind: "control" };
    return {
      kind: "tunnel",
      target: { machineId: m[1], port: parseInt(m[2], 10), forwardPath: pathname },
    };
  }

  // path 模式
  if (pathname === "/relay" || pathname === "/relay/") return { kind: "control" };
  const pm = pathname.match(PATH_TUNNEL_RE);
  if (pm) {
    return {
      kind: "tunnel",
      target: { machineId: pm[1], port: parseInt(pm[2], 10), forwardPath: pm[3] || "/" },
    };
  }
  const cm = (cookieHeader || "").match(PATH_COOKIE_RE);
  if (cm) {
    return {
      kind: "tunnel",
      target: { machineId: cm[1], port: parseInt(cm[2], 10), forwardPath: pathname },
    };
  }
  return { kind: "none" };
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/tunnelAddress.test.ts`
Expected: PASS(全部用例)。

- [ ] **Step 5: Commit**

```bash
git add packages/relay/src/tunnelAddress.ts packages/relay/src/tunnelAddress.test.ts
git commit -m "feat(relay): tunnelAddress 纯函数(两 router 共用寻址,子域+路径)"
```

---

### Task 4: redirectRewrite.ts —— 仅路径模式补 Location 前缀

**Files:**
- Create: `packages/relay/src/redirectRewrite.ts`
- Test: `packages/relay/src/redirectRewrite.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  ```ts
  export function rewriteRedirectHeaders(
    headers: Record<string, string | string[]>,
    machineId: string,
    port: number
  ): Record<string, string | string[]>;
  ```
  只可能改动 `Location` 一个键(站内绝对路径补 `/t/<machineId>/<port>` 前缀),**其余头(含所有 Set-Cookie)原样**。返回新对象(不原地改)。

**背景:** 见 spec「文件结构 › redirectRewrite.ts」+「非目标」。只补 Location,绝不碰 Set-Cookie(`pc_tunnel` 保持 `Path=/` 兜底子资源)。

- [ ] **Step 1: 写失败测试**

创建 `packages/relay/src/redirectRewrite.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { rewriteRedirectHeaders } from "./redirectRewrite.js";

const ID = "bda488cfdee21ccc";
const PORT = 3000;
const PFX = `/t/${ID}/${PORT}`;

describe("rewriteRedirectHeaders", () => {
  it("站内绝对路径 Location 补前缀", () => {
    const out = rewriteRedirectHeaders({ location: "/admin" }, ID, PORT);
    expect(out.location).toBe(`${PFX}/admin`);
  });
  it("大小写 Location 键都处理(header 名不区分大小写)", () => {
    const out = rewriteRedirectHeaders({ Location: "/admin" }, ID, PORT);
    // 归一后仍能被浏览器识别;断言值被补前缀(键名保留原样或归一均可,只要值对)
    const val = (out.Location ?? out.location) as string;
    expect(val).toBe(`${PFX}/admin`);
  });
  it("相对路径不动(浏览器基于当前路径解析)", () => {
    const out = rewriteRedirectHeaders({ location: "admin/sub" }, ID, PORT);
    expect(out.location).toBe("admin/sub");
  });
  it("绝对 URL(scheme://)不动", () => {
    const out = rewriteRedirectHeaders({ location: "https://other.com/x" }, ID, PORT);
    expect(out.location).toBe("https://other.com/x");
  });
  it("protocol-relative //host 不动", () => {
    const out = rewriteRedirectHeaders({ location: "//evil.com/x" }, ID, PORT);
    expect(out.location).toBe("//evil.com/x");
  });
  it("无 Location 时 headers 原样返回", () => {
    const input = { "content-type": "text/html", "set-cookie": "a=1; Path=/" };
    expect(rewriteRedirectHeaders(input, ID, PORT)).toEqual(input);
  });
  it("Set-Cookie 一律不改(含 pc_tunnel / pc_tunnel_token / 应用 cookie)", () => {
    const input = {
      location: "/admin",
      "set-cookie": ["pc_tunnel=x:3000; Path=/", "pc_tunnel_token=t; Path=/; HttpOnly", "app_sess=z; Path=/admin"],
    };
    const out = rewriteRedirectHeaders(input, ID, PORT);
    expect(out.location).toBe(`${PFX}/admin`);
    expect(out["set-cookie"]).toEqual(input["set-cookie"]); // 逐条原样
  });
  it("不原地改输入对象", () => {
    const input = { location: "/admin" };
    rewriteRedirectHeaders(input, ID, PORT);
    expect(input.location).toBe("/admin"); // 原对象未被改
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/redirectRewrite.test.ts`
Expected: FAIL — 模块不存在。

- [ ] **Step 3: 实现**

创建 `packages/relay/src/redirectRewrite.ts`:

```ts
// ── 路径模式:补 Location 前缀 ──────────────────────────────
// 仅当 Location 是"站内绝对路径"(/ 开头、非 //、非 scheme://)时补 /t/<id>/<port>
// 前缀,让服务端 3xx 重定向不掉出隧道前缀。绝不改写任何 Set-Cookie
// (pc_tunnel 需保持 Path=/ 兜底裸绝对路径子资源)。子域模式不调用本函数。

/** 站内绝对路径:/ 开头,但不是 //host(protocol-relative)。 */
function isSiteAbsolute(loc: string): boolean {
  return loc.startsWith("/") && !loc.startsWith("//");
}

export function rewriteRedirectHeaders(
  headers: Record<string, string | string[]>,
  machineId: string,
  port: number
): Record<string, string | string[]> {
  const prefix = `/t/${machineId}/${port}`;
  const out: Record<string, string | string[]> = {};
  for (const [k, v] of Object.entries(headers)) {
    if (k.toLowerCase() === "location" && typeof v === "string" && isSiteAbsolute(v)) {
      out[k] = prefix + v;
    } else {
      out[k] = v;
    }
  }
  return out;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/redirectRewrite.test.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add packages/relay/src/redirectRewrite.ts packages/relay/src/redirectRewrite.test.ts
git commit -m "feat(relay): redirectRewrite 纯函数(路径模式仅补 Location 前缀,不碰 Set-Cookie)"
```

---

### Task 5: daemon fetch 改 redirect:manual(3xx 透传)

**Files:**
- Modify: `packages/tunnel-client/src/tunnel.ts:43`
- Test: `packages/tunnel-client/src/tunnel.test.ts`

**Interfaces:**
- Consumes: 无
- Produces: `proxyToLocalhost` 遇 3xx 不再本地跟随,原样成帧(status=3xx,保留 `Location` 头,body 通常空)回传 relay。

**背景:** 见 spec「文件结构 › tunnel.ts」+「取证结论」。收编 daemon fetch 吞 307 的偏差,是补前缀与子域重定向正确的共同前置。

- [ ] **Step 1: 写失败测试**

在 `packages/tunnel-client/src/tunnel.test.ts` 的 `describe("proxyToLocalhost", ...)` 内追加(照搬现有 encoding 测试的 fakeFetch 模式):

```ts
  it("3xx 透传:redirect:manual 下不跟随,保留 Location、成帧原状态", async () => {
    // fake fetch 断言 init.redirect,并返回 307+Location(明文空 body)
    let seenRedirect: RequestRedirect | undefined;
    const fakeFetch = (async (_url: string, init: RequestInit) => {
      seenRedirect = init.redirect;
      return new Response(null, {
        status: 307,
        headers: { location: "/admin", "content-type": "text/plain" },
      });
    }) as unknown as typeof fetch;

    const frames: HttpReplyFrame[] = [];
    await proxyToLocalhost(
      { tunnelId: "t_redir", port: 1, method: "GET", path: "/", headers: {} },
      collect(frames),
      fakeFetch
    );
    expect(seenRedirect).toBe("manual"); // 关键:daemon 要求不跟随
    const resp = frames.find((f) => f.type === "tunnel-response") as any;
    expect(resp.status).toBe(307);
    expect(resp.headers["location"]).toBe("/admin"); // Location 原样回传
    expect((frames.at(-1) as any).error).toBeUndefined(); // 空 body 也正常 end
  });
```

> 注:`collect`、`HttpReplyFrame` 已在该测试文件顶部 import/定义;沿用即可。`RequestRedirect` 是 TS 内置全局类型,无需 import。

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @pocket-code/tunnel-client exec vitest run src/tunnel.test.ts`
Expected: FAIL — 当前 `init` 无 `redirect`,`seenRedirect` 为 `undefined` ≠ `"manual"`(且默认 follow 会跟随)。

- [ ] **Step 3: 改 tunnel.ts:43**

`packages/tunnel-client/src/tunnel.ts` 第 43 行:

```ts
    const init: RequestInit = { method: req.method, headers: reqHeaders, redirect: "manual" };
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @pocket-code/tunnel-client exec vitest run src/tunnel.test.ts`
Expected: PASS(含原有全部 tunnel 测试)。

- [ ] **Step 5: Commit**

```bash
git add packages/tunnel-client/src/tunnel.ts packages/tunnel-client/src/tunnel.test.ts
git commit -m "fix(tunnel): daemon fetch redirect:manual(3xx+Location 透传,不本地跟随)"
```

---

### Task 6: 接线 relay HTTP —— tunnelAddress + redirectRewrite + /health + deps

这是集成任务:把 Task 2/3/4 的纯函数接进 HTTP 入口,并把 mode/baseDomain 从 index.ts 注入。

**Files:**
- Modify: `packages/relay/src/tunnelHub.ts`(加 rewrite 回调挂点)
- Modify: `packages/relay/src/httpRouter.ts`(deps + 寻址替换 + /health + open 回调)
- Modify: `packages/relay/src/index.ts`(读 mode/baseDomain + 快速失败 + 注入)
- Test: `packages/relay/src/httpRouter.test.ts`、`packages/relay/src/tunnelHub.test.ts`(若存在;不存在则新增最小用例文件)

**Interfaces:**
- Consumes: `resolveTunnelTarget`/`TunnelTarget`(Task 3)、`rewriteRedirectHeaders`(Task 4)、`getTunnelMode`/`getTunnelBaseDomain`/`TunnelMode`(Task 2)。
- Produces: `HttpRouterDeps` 新增字段 `tunnelMode: TunnelMode`、`tunnelBaseDomain: string | null`。`TunnelHub.open` 新增可选第 5 参 `rewriteHeaders?: (h: Record<string,string|string[]>) => Record<string,string|string[]>`。

**背景:** redirectRewrite 的挂点必须在 `TunnelHub.onResponse`——那是唯一同时握有"响应头"与"该隧道的 machineId+port+mode 上下文"的地方(响应帧到达 messageRouter 时只带 tunnelId)。所以在 `open()` 时把一个已绑定 machineId/port 的 rewrite 回调存进 `PendingTunnel`,`onResponse` 在 `writeHead` 前应用它。子域模式不传回调(no-op)。

- [ ] **Step 1: 写失败测试(tunnelHub 应用 rewrite 回调)**

确认 `packages/relay/src/tunnelHub.test.ts` 是否存在。若不存在则创建;若存在则在其中追加:

```ts
import { describe, it, expect } from "vitest";
import { ServerResponse, IncomingMessage } from "http";
import { Socket } from "net";
import { TunnelHub } from "./tunnelHub.js";

function fakeRes() {
  const res = new ServerResponse(new IncomingMessage(new Socket()));
  const captured: { status?: number; headers?: any } = {};
  (res as any).writeHead = (s: number, h: any) => { captured.status = s; captured.headers = h; return res; };
  (res as any).write = () => true;
  (res as any).end = () => res;
  return { res, captured };
}

describe("TunnelHub.onResponse rewrite 回调", () => {
  it("open 传入 rewriteHeaders 时,onResponse 用它改写后再 writeHead", () => {
    const hub = new TunnelHub();
    const { res, captured } = fakeRes();
    hub.open("t1", res, "m1", undefined, (h) => ({ ...h, location: "/t/m1/3000" + (h.location ?? "") }));
    hub.onResponse("t1", 307, { location: "/admin" }, "m1");
    expect(captured.status).toBe(307);
    expect(captured.headers.location).toBe("/t/m1/3000/admin");
  });
  it("无 rewriteHeaders 时 onResponse 原样写头(现状回归)", () => {
    const hub = new TunnelHub();
    const { res, captured } = fakeRes();
    hub.open("t2", res, "m1");
    hub.onResponse("t2", 200, { "content-type": "text/plain" }, "m1");
    expect(captured.status).toBe(200);
    expect(captured.headers["content-type"]).toBe("text/plain");
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/tunnelHub.test.ts`
Expected: FAIL — `open` 只接受 4 参,第 5 参 rewrite 未实现/未应用。

- [ ] **Step 3: 改 tunnelHub.ts(加 rewrite 回调挂点)**

`packages/relay/src/tunnelHub.ts`:

1. `PendingTunnel` 接口加字段:

```ts
interface PendingTunnel {
  res: ServerResponse;
  responded: boolean;
  machineId: string;
  extraHeaders?: Record<string, string | string[]>;
  /** 路径模式:写响应头前对头做补前缀改写(子域模式不传)。 */
  rewriteHeaders?: (h: Record<string, string | string[]>) => Record<string, string | string[]>;
}
```

2. `open` 签名加第 5 参:

```ts
  open(
    tunnelId: string,
    res: ServerResponse,
    machineId: string,
    extraHeaders?: Record<string, string | string[]>,
    rewriteHeaders?: (h: Record<string, string | string[]>) => Record<string, string | string[]>
  ): void {
    this.tunnels.set(tunnelId, { res, responded: false, machineId, extraHeaders, rewriteHeaders });
  }
```

3. `onResponse` 在注入 extraHeaders 之后、`writeHead` 之前应用 rewrite:

```ts
    // 注入 relay 附加头(如下发 pc_tunnel cookie)。
    if (t.extraHeaders) Object.assign(safe, t.extraHeaders);
    // 路径模式:补 Location 前缀(子域模式 rewriteHeaders 为空,跳过)。
    const finalHeaders = t.rewriteHeaders ? t.rewriteHeaders(safe) : safe;
    try {
      t.res.writeHead(status, finalHeaders);
    } catch {
      /* res 已结束/出错,忽略 */
    }
```

- [ ] **Step 4: 运行确认 tunnelHub 测试通过**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/tunnelHub.test.ts`
Expected: PASS。

- [ ] **Step 5: 写失败测试(httpRouter 两模式 + /health)**

`packages/relay/src/httpRouter.test.ts`:先改 `startHttp` 帮手支持传 mode/baseDomain,再加用例。

改 `startHttp` 签名(默认值保持现状回归——注意:**默认 mode 设为 `"path"`** 以让现有全部用例继续走路径模式,不必逐个改):

```ts
async function startHttp(
  tunnelToken: string | null,
  tunnelCookieSecure = true,
  tunnelMode: "subdomain" | "path" = "path",
  tunnelBaseDomain: string | null = null
): Promise<Ctx> {
  const hub = new TunnelHub();
  const frames: any[] = [];
  const server = createServer(
    createHttpHandler({
      tunnelHub: hub,
      sendToDaemon: (machineId, frame) => { frames.push({ machineId, frame }); return true; },
      getOnlineMachineCount: () => 1,
      port: 0,
      tunnelToken,
      tunnelCookieSecure,
      tunnelMode,
      tunnelBaseDomain,
    })
  );
  await new Promise<void>((r) => server.listen(0, () => r()));
  const addr = server.address();
  return { server, hub, frames, port: typeof addr === "object" && addr ? addr.port : 0 };
}
```

追加用例:

```ts
  it("/health 暴露 tunnelMode + tunnelBaseDomain", async () => {
    const ctx = await startHttp(null, true, "subdomain", "tunnel.aigc.zj.cn"); ctxs.push(ctx);
    const j = await (await fetch(`http://127.0.0.1:${ctx.port}/health`)).json();
    expect(j.tunnelMode).toBe("subdomain");
    expect(j.tunnelBaseDomain).toBe("tunnel.aigc.zj.cn");
  });

  it("subdomain 模式:Host 驱动路由,forwardPath=pathname,不下发 pc_tunnel", async () => {
    const ctx = await startHttp(null, true, "subdomain", "tunnel.aigc.zj.cn"); ctxs.push(ctx);
    const resp = await fetchViaTunnel(ctx, "/admin", { host: "bda488cfdee21ccc-3000.tunnel.aigc.zj.cn" });
    expect(resp.status).toBe(200);
    expect(ctx.frames[0].frame.path).toBe("/admin");
    expect(ctx.frames[0].machineId).toBe("bda488cfdee21ccc");
    expect(resp.headers.getSetCookie().join(";")).not.toContain("pc_tunnel="); // 子域不下发
  });

  it("subdomain 模式:主站 Host → 隧道不触发(无 tunnel-request 帧)", async () => {
    const ctx = await startHttp(null, true, "subdomain", "tunnel.aigc.zj.cn"); ctxs.push(ctx);
    const resp = await fetch(`http://127.0.0.1:${ctx.port}/whatever`, { headers: { host: "aigc.zj.cn" } });
    expect(resp.status).toBe(404); // control→非 /health→非隧道→404
    expect(ctx.frames).toHaveLength(0);
  });

  it("path 模式:3xx Location 被补前缀,Set-Cookie(pc_tunnel)不被改写", async () => {
    const ctx = await startHttp(null, true, "path"); ctxs.push(ctx);
    // 手动驱动一次隧道,用 hub 回一个 307+Location + 一条应用 Set-Cookie
    const respP = fetch(`http://127.0.0.1:${ctx.port}/t/bda488cfdee21ccc/3000/`, { redirect: "manual" });
    const t0 = Date.now();
    while (!ctx.frames.length && Date.now() - t0 < 2000) await new Promise((r) => setTimeout(r, 10));
    const { machineId, frame } = ctx.frames[0];
    ctx.hub.onResponse(frame.tunnelId, 307, { location: "/admin", "set-cookie": "app=1; Path=/" }, machineId);
    ctx.hub.onEnd(frame.tunnelId, undefined, machineId);
    const resp = await respP;
    expect(resp.status).toBe(307);
    expect(resp.headers.get("location")).toBe("/t/bda488cfdee21ccc/3000/admin");
    // pc_tunnel 路由 cookie 仍 Path=/(下发点),应用 cookie 原样
    const setc = resp.headers.getSetCookie().join(";;");
    expect(setc).toContain("pc_tunnel=bda488cfdee21ccc:3000; Path=/");
    expect(setc).toContain("app=1; Path=/");
  });
```

> `fetchViaTunnel` 现签名是 `(ctx, path, headers)`,已支持传 headers(用于 `host`)。确认其内部用 `redirect:"manual"` 并对首帧回 200——现状即如此,子域用例复用无碍。

- [ ] **Step 6: 运行确认失败**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/httpRouter.test.ts`
Expected: FAIL — `createHttpHandler` 的 deps 尚无 `tunnelMode`/`tunnelBaseDomain`;寻址仍是旧内联逻辑;/health 无新字段。

- [ ] **Step 7: 改 httpRouter.ts**

`packages/relay/src/httpRouter.ts` 全面改造:

1. import:

```ts
import { verifyTunnelToken, type TunnelMode } from "./config.js";
import { resolveTunnelTarget } from "./tunnelAddress.js";
import { rewriteRedirectHeaders } from "./redirectRewrite.js";
```

2. `HttpRouterDeps` 加字段:

```ts
  tunnelMode: TunnelMode;
  tunnelBaseDomain: string | null;
```

3. `/health` 响应加两字段:

```ts
    if (url.pathname === "/health") {
      res.writeHead(200, { "Content-Type": "application/json" });
      res.end(
        JSON.stringify({
          status: "ok",
          timestamp: Date.now(),
          machines: deps.getOnlineMachineCount(),
          tunnelMode: deps.tunnelMode,
          tunnelBaseDomain: deps.tunnelBaseDomain,
        })
      );
      return;
    }
```

4. 用 `resolveTunnelTarget` 替换 `tunnelMatch` / `cookieMatch` 两段内联逻辑。删除原第 90-116 行(`/t/` 显式分支 + cookie 兜底分支 + 末尾 notFound),替换为:

```ts
    // ── 统一寻址(两模式共用) ──
    const resolved = resolveTunnelTarget(
      deps.tunnelMode,
      req.headers.host,
      url.pathname,
      req.headers.cookie,
      deps.tunnelBaseDomain
    );
    if (resolved.kind !== "tunnel") return notFound(); // control/none 都不是本 relay 的 HTTP 隧道请求
    const { machineId, port, forwardPath } = resolved.target;
    if (!tokenOk) return notFound();

    // 路径模式且命中隧道:下发 pc_tunnel 路由 cookie(Path=/ 兜底子资源)
    // + 首次带 pc_token 校验通过时种 pc_tunnel_token。子域模式不下发、不补前缀。
    let extraHeaders: Record<string, string | string[]> | undefined;
    let rewrite: ((h: Record<string, string | string[]>) => Record<string, string | string[]>) | undefined;
    if (deps.tunnelMode === "path") {
      const setCookies: string[] = [`pc_tunnel=${machineId}:${port}; Path=/; SameSite=Lax`];
      if (deps.tunnelToken !== null && queryToken && verifyTunnelToken(deps.tunnelToken, queryToken)) {
        const secure = deps.tunnelCookieSecure ? "; Secure" : "";
        setCookies.push(`pc_tunnel_token=${queryToken}; Path=/; HttpOnly; SameSite=Lax${secure}`);
      }
      extraHeaders = { "Set-Cookie": setCookies };
      rewrite = (h) => rewriteRedirectHeaders(h, machineId, port);
    }

    startTunnel(machineId, port, forwardPath + search, extraHeaders, rewrite);
    return;
```

> `extraHeaders` 的 `Set-Cookie` 值是 `string[]`,与 `startTunnel`/`hub.open` 的 `Record<string, string | string[]>` 类型一致。子域模式下 `extraHeaders`/`rewrite` 均为 `undefined`——不下发 pc_tunnel、onResponse 不改写头。

5. `startTunnel` 签名加第 5 参 `rewrite`,并透传给 `hub.open`:

```ts
    const startTunnel = (
      machineId: string,
      port: number,
      path: string,
      extraHeaders?: Record<string, string | string[]>,
      rewrite?: (h: Record<string, string | string[]>) => Record<string, string | string[]>
    ) => {
      const tunnelId = crypto.randomUUID();
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(req.headers)) {
        if (typeof v === "string") headers[k] = v;
        else if (Array.isArray(v)) headers[k] = v.join(", ");
      }
      const chunks: Buffer[] = [];
      req.on("data", (c) => chunks.push(c as Buffer));
      req.on("end", () => {
        const body = chunks.length ? Buffer.concat(chunks).toString("base64") : undefined;
        deps.tunnelHub.open(tunnelId, res, machineId, extraHeaders, rewrite);
        const ok = deps.sendToDaemon(machineId, {
          type: "tunnel-request", tunnelId, port,
          method: req.method || "GET", path, headers, body,
        });
        if (!ok) deps.tunnelHub.onEnd(tunnelId, `daemon ${machineId} offline`);
      });
    };
```

> 删掉原来 `const [, machineId, portStr, rest] = tunnelMatch` 那一整套显式分支与 cookie 分支;`queryToken`/`cookieToken`/`tokenOk`/`search`/`url.searchParams.delete("pc_token")` 这些在函数上部已算好的保持不动(它们在寻址之前)。保留 `TUNNEL_TOKEN_COOKIE_RE`(鉴权 cookie 仍要读),删除不再使用的 `TUNNEL_COOKIE_RE`(路由 cookie 匹配已移入 tunnelAddress)。

- [ ] **Step 8: 改 index.ts(读 mode/baseDomain + 快速失败 + 注入)**

`packages/relay/src/index.ts`:

1. import 加 `getTunnelMode, getTunnelBaseDomain`:

```ts
import { requireRelaySecret, isDiscoveryEnabled, getTunnelToken, isTunnelCookieSecure, getTunnelMode, getTunnelBaseDomain } from "./config.js";
```

2. 在 `TUNNEL_COOKIE_SECURE` 那段之后加读取 + 快速失败:

```ts
const TUNNEL_MODE = getTunnelMode();
const TUNNEL_BASE_DOMAIN = getTunnelBaseDomain();
if (TUNNEL_MODE === "subdomain" && TUNNEL_BASE_DOMAIN === null) {
  console.error("[Relay] 启动失败:TUNNEL_BASE_DOMAIN required when TUNNEL_MODE=subdomain (e.g. tunnel.example.com)");
  process.exit(1);
}
console.log(`[Relay] Tunnel mode: ${TUNNEL_MODE}${TUNNEL_MODE === "subdomain" ? ` (base=${TUNNEL_BASE_DOMAIN})` : ""}`);
```

3. `createHttpHandler` deps 加两字段:

```ts
  createHttpHandler({
    tunnelHub,
    sendToDaemon: sendRawToDaemon,
    getOnlineMachineCount: () => getOnlineMachines().length,
    port: PORT,
    tunnelToken: TUNNEL_TOKEN,
    tunnelCookieSecure: TUNNEL_COOKIE_SECURE,
    tunnelMode: TUNNEL_MODE,
    tunnelBaseDomain: TUNNEL_BASE_DOMAIN,
  })
```

4. `createUpgradeHandler` deps 也加两字段(Task 7 会用,这里一并注入避免二次改 index):

```ts
  createUpgradeHandler({
    controlWss: wss,
    tunnelWss,
    wsTunnelHub,
    sendToDaemon: sendRawToDaemon,
    port: PORT,
    tunnelToken: TUNNEL_TOKEN,
    tunnelMode: TUNNEL_MODE,
    tunnelBaseDomain: TUNNEL_BASE_DOMAIN,
  })
```

> 若 Task 7 尚未改 `UpgradeDeps` 接口,此处 TS 会报未知字段。**顺序保证**:本 Task 6 先只加 httpRouter 的注入;upgradeRouter 的两字段注入放到 Task 7 与接口改动同一提交。**故 Step 8.4 移到 Task 7 执行**——本 Task 仅做 8.1/8.2/8.3。

- [ ] **Step 9: 运行确认全绿**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/httpRouter.test.ts src/tunnelHub.test.ts`
Expected: PASS(含现有全部 httpRouter 用例——它们默认走 `"path"` 模式,行为不变)。

- [ ] **Step 10: relay 全量测试回归(确保没碰坏 e2e/messageRouter)**

Run: `pnpm --filter @pocket-code/relay test`
Expected: PASS(e2e 现有场景仍走路径模式默认;若 e2e 因 index.ts 现在要求 subdomain+baseDomain 而起不来——注意 e2e 的场景 A/B **未设 TUNNEL_MODE**,默认 subdomain 会快速失败!这是必须处理的交互,见下步)。

- [ ] **Step 11: 修 e2e 现有场景的模式(默认 subdomain 会让它们快速失败)**

因默认 mode=subdomain 且 e2e 场景未设 baseDomain 会 `exit(1)`,给现有两个场景的 spawnRelay env 显式加 `TUNNEL_MODE: "path"`:

`packages/relay/src/e2e.tunnel.test.ts` 场景 A(第 72 行附近):

```ts
      relay = spawnRelay({ PORT: String(relayPort), RELAY_SECRET: SECRET, RELAY_DISCOVERY: "off", TUNNEL_TOKEN: "", TUNNEL_MODE: "path" });
```

场景 B(第 116 行附近):

```ts
      relay = spawnRelay({ PORT: String(relayPort), RELAY_SECRET: SECRET, TUNNEL_TOKEN: TOKEN, RELAY_DISCOVERY: "", TUNNEL_MODE: "path" });
```

同时把两场景的 `machineId: "m_e2e_a"`/`"m_e2e_b"` 改为无下划线(hex 化前缀 App 无关,但保持一致性 + 为 Task 9 子域场景铺路):`"e2eaaa1"` / `"e2ebbb2"`,并同步更新用例里的 `/t/m_e2e_a/` → `/t/e2eaaa1/`、cookie `pc_tunnel=m_e2e_a:` → `pc_tunnel=e2eaaa1:` 等所有引用。

> 逐一替换该文件内 `m_e2e_a`→`e2eaaa1`、`m_e2e_b`→`e2ebbb2`(共约 6 处)。

- [ ] **Step 12: 运行 e2e 确认通过**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/e2e.tunnel.test.ts`
Expected: PASS(两场景走显式 path 模式)。

- [ ] **Step 13: Commit**

```bash
git add packages/relay/src/tunnelHub.ts packages/relay/src/tunnelHub.test.ts packages/relay/src/httpRouter.ts packages/relay/src/httpRouter.test.ts packages/relay/src/index.ts packages/relay/src/e2e.tunnel.test.ts
git commit -m "feat(relay): HTTP 入口接双模式(tunnelAddress 寻址+redirectRewrite 补 Location+/health 暴露 mode)"
```

---

### Task 7: 接线 relay WS-upgrade —— tunnelAddress 统一寻址

**Files:**
- Modify: `packages/relay/src/upgradeRouter.ts`
- Modify: `packages/relay/src/index.ts`(补 upgrade deps 的两字段——即 Task 6 Step 8.4)
- Test: `packages/relay/src/upgradeRouter.test.ts`(若不存在则创建)

**Interfaces:**
- Consumes: `resolveTunnelTarget`(Task 3)、`TunnelMode`(Task 2)。
- Produces: `UpgradeDeps` 加 `tunnelMode: TunnelMode`、`tunnelBaseDomain: string | null`。WS 寻址与 HTTP 同语义。

**背景:** Global Constraint 要求两入口共享寻址。WS 无响应头改写需求(升级响应不含 Location),故不涉及 redirectRewrite。

- [ ] **Step 1: 写失败测试(WS 两模式寻址)**

确认 `packages/relay/src/upgradeRouter.test.ts` 是否存在。若不存在则创建;测试 `dispatchUpgrade` 的路由决策——用真实 http server + ws 客户端较重,这里改测**寻址判定**是否把请求分到 tunnel/control。最小可行:起一个挂了 `createUpgradeHandler` 的 http server,用两个 controlWss/tunnelWss 各自 `connection` 事件计数,发起 ws 握手断言落到哪个 wss。

```ts
import { describe, it, expect, afterEach } from "vitest";
import { createServer, type Server } from "http";
import { WebSocketServer } from "ws";
import WebSocket from "ws";
import { createUpgradeHandler, makeTunnelWss } from "./upgradeRouter.js";
import { WsTunnelHub } from "./wsTunnelHub.js";

interface Ctx { server: Server; port: number; controlHits: string[]; tunnelHits: number; }
const ctxs: Ctx[] = [];
afterEach(() => { for (const c of ctxs.splice(0)) c.server.close(); });

async function startWs(mode: "subdomain" | "path", baseDomain: string | null): Promise<Ctx> {
  const controlWss = new WebSocketServer({ noServer: true });
  const tunnelWss = makeTunnelWss();
  const ctx: Ctx = { server: null as any, port: 0, controlHits: [], tunnelHits: 0 };
  controlWss.on("connection", (_ws, req) => ctx.controlHits.push(req.url || ""));
  const wsTunnelHub = new WsTunnelHub(() => true); // sendToDaemon 恒真,open 后不真正连
  tunnelWss.on("connection", () => { ctx.tunnelHits++; });
  const server = createServer();
  server.on("upgrade", createUpgradeHandler({
    controlWss, tunnelWss, wsTunnelHub,
    sendToDaemon: () => true, port: 0,
    tunnelToken: null, tunnelMode: mode, tunnelBaseDomain: baseDomain,
  }));
  await new Promise<void>((r) => server.listen(0, () => r()));
  const addr = server.address();
  ctx.server = server; ctx.port = typeof addr === "object" && addr ? addr.port : 0;
  return ctx;
}

function tryWs(port: number, path: string, host?: string): Promise<void> {
  return new Promise((resolve) => {
    const ws = new WebSocket(`ws://127.0.0.1:${port}${path}`, host ? { headers: { host } } : {});
    ws.on("open", () => { ws.close(); resolve(); });
    ws.on("error", () => resolve()); // 404/destroy 也算完成
    setTimeout(resolve, 800);
  });
}

describe("createUpgradeHandler 寻址(两模式)", () => {
  it("path 模式:/relay → control", async () => {
    const ctx = await startWs("path", null); ctxs.push(ctx);
    await tryWs(ctx.port, "/relay");
    expect(ctx.controlHits.some((u) => u.startsWith("/relay"))).toBe(true);
  });
  it("path 模式:/t/abc123/5173/ → tunnel", async () => {
    const ctx = await startWs("path", null); ctxs.push(ctx);
    await tryWs(ctx.port, "/t/abc123/5173/");
    expect(ctx.tunnelHits).toBe(1);
  });
  it("subdomain 模式:隧道子域 Host → tunnel", async () => {
    const ctx = await startWs("subdomain", "tunnel.aigc.zj.cn"); ctxs.push(ctx);
    await tryWs(ctx.port, "/", "aa11bb22-5173.tunnel.aigc.zj.cn");
    expect(ctx.tunnelHits).toBe(1);
  });
  it("subdomain 模式:控制 Host(主站)+ /relay → control", async () => {
    const ctx = await startWs("subdomain", "tunnel.aigc.zj.cn"); ctxs.push(ctx);
    await tryWs(ctx.port, "/relay", "aigc.zj.cn");
    expect(ctx.controlHits.some((u) => u.startsWith("/relay"))).toBe(true);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/upgradeRouter.test.ts`
Expected: FAIL — `UpgradeDeps` 无 `tunnelMode`/`tunnelBaseDomain`;subdomain 寻址未实现。

- [ ] **Step 3: 改 upgradeRouter.ts**

`packages/relay/src/upgradeRouter.ts`:

1. import:

```ts
import { verifyTunnelToken, type TunnelMode } from "./config.js";
import { resolveTunnelTarget } from "./tunnelAddress.js";
```

2. `UpgradeDeps` 加字段:

```ts
  tunnelMode: TunnelMode;
  tunnelBaseDomain: string | null;
```

3. 替换 `dispatchUpgrade` 里的内联寻址(第 59-76 行:`machineId/port/path` 三变量 + `isControlPath` + `TUNNEL_PATH_RE`/`TUNNEL_COOKIE_RE` 分支):

```ts
    const url = new URL(req.url || "", `http://localhost:${deps.port || 80}`);

    const resolved = resolveTunnelTarget(
      deps.tunnelMode,
      req.headers.host,
      url.pathname,
      req.headers.cookie,
      deps.tunnelBaseDomain
    );

    let machineId: string | null = null;
    let port = 0;
    let path = "";
    if (resolved.kind === "tunnel") {
      machineId = resolved.target.machineId;
      port = resolved.target.port;
      path = resolved.target.forwardPath + (url.search || "");
    }
    // control / none 都不当隧道:落控制通道(与原 isControlPath 行为一致——
    // 非隧道的 upgrade 交给 controlWss,由其协议层决定是否接纳)。
```

> 保留后续 `if (machineId) { 鉴权 + pc_token 剥离 }` 与 `if (!machineId) { controlWss.handleUpgrade }` 两段不变。删除顶部不再使用的 `TUNNEL_PATH_RE`/`TUNNEL_COOKIE_RE` 常量(`TUNNEL_TOKEN_COOKIE_RE` 保留,鉴权仍用)。

4. 删除 `dispatchUpgrade` 里现存的 `const url = new URL(...)`(已在上面重建),避免重复声明。

- [ ] **Step 4: 补 index.ts 的 upgrade deps 两字段(Task 6 Step 8.4)**

`packages/relay/src/index.ts` 的 `createUpgradeHandler({...})` 加:

```ts
    tunnelMode: TUNNEL_MODE,
    tunnelBaseDomain: TUNNEL_BASE_DOMAIN,
```

- [ ] **Step 5: 运行确认通过**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/upgradeRouter.test.ts`
Expected: PASS。

- [ ] **Step 6: relay 全量回归**

Run: `pnpm --filter @pocket-code/relay test`
Expected: PASS(全部)。

- [ ] **Step 7: Commit**

```bash
git add packages/relay/src/upgradeRouter.ts packages/relay/src/upgradeRouter.test.ts packages/relay/src/index.ts
git commit -m "feat(relay): WS-upgrade 入口接双模式寻址(与 HTTP 共用 tunnelAddress)"
```

---

### Task 8: App PreviewTab 模式感知拼 URL

**Files:**
- Create: `packages/app/src/components/PreviewTab/tunnelUrl.ts`
- Create: `packages/app/src/components/PreviewTab/tunnelUrl.test.ts`
- Modify: `packages/app/src/components/PreviewTab/index.tsx`(改为 import tunnelUrl.ts + 拉 /health 缓存)
- Modify: `packages/app/src/store/settings.ts`(加两运行时字段)

**Interfaces:**
- Consumes: `/health` 返回的 `{ tunnelMode, tunnelBaseDomain }`(Task 6)。
- Produces:
  ```ts
  export interface TunnelInfo { mode: "subdomain" | "path"; baseDomain: string | null; }
  export function buildTunnelUrl(relayServerUrl: string, machineId: string, port: number, info?: TunnelInfo): string;
  export function maybeRewriteToTunnel(input: string, settings: { workspaceMode?: string; relayServerUrl?: string; relayMachineId?: string; relayTunnelMode?: "subdomain"|"path"; relayTunnelBaseDomain?: string | null } | undefined): string | null;
  ```

**背景:** 见 spec「文件结构 › PreviewTab」。现有 `buildTunnelUrl`/`maybeRewriteToTunnel` 是模块私有、不可测——抽到独立文件并导出(URL 逻辑与 React 组件解耦,亦满足测试要求)。

- [ ] **Step 1: 写失败测试**

创建 `packages/app/src/components/PreviewTab/tunnelUrl.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { buildTunnelUrl, maybeRewriteToTunnel } from "./tunnelUrl.js";

describe("buildTunnelUrl", () => {
  it("默认/path 模式:拼 /t/<id>/<port>/", () => {
    expect(buildTunnelUrl("wss://aigc.zj.cn/relay", "abc123", 3000))
      .toBe("https://aigc.zj.cn/relay/t/abc123/3000/");
  });
  it("path 模式显式:同上", () => {
    expect(buildTunnelUrl("wss://aigc.zj.cn/relay", "abc123", 3000, { mode: "path", baseDomain: null }))
      .toBe("https://aigc.zj.cn/relay/t/abc123/3000/");
  });
  it("subdomain 模式:拼 <id>-<port>.<baseDomain>", () => {
    expect(buildTunnelUrl("wss://aigc.zj.cn/relay", "abc123", 3000, { mode: "subdomain", baseDomain: "tunnel.aigc.zj.cn" }))
      .toBe("https://abc123-3000.tunnel.aigc.zj.cn/");
  });
  it("subdomain 模式缺 baseDomain → 回退 path", () => {
    expect(buildTunnelUrl("ws://h:3200/relay", "abc123", 5173, { mode: "subdomain", baseDomain: null }))
      .toBe("http://h:3200/relay/t/abc123/5173/");
  });
  it("ws→http / wss→https 协议映射", () => {
    expect(buildTunnelUrl("ws://h:3200", "id0", 8080, { mode: "subdomain", baseDomain: "t.example.com" }))
      .toBe("http://id0-8080.t.example.com/");
  });
});

const S = (over: object) => ({ workspaceMode: "relay", relayServerUrl: "wss://aigc.zj.cn/relay", relayMachineId: "abc123", ...over });

describe("maybeRewriteToTunnel", () => {
  it("非 relay 模式返回 null", () => {
    expect(maybeRewriteToTunnel("3000", { workspaceMode: "local" })).toBeNull();
  });
  it("裸端口 → path 隧道 URL", () => {
    expect(maybeRewriteToTunnel("3000", S({}))).toBe("https://aigc.zj.cn/relay/t/abc123/3000/");
  });
  it("裸端口 + subdomain info → 子域 URL", () => {
    expect(maybeRewriteToTunnel("3000", S({ relayTunnelMode: "subdomain", relayTunnelBaseDomain: "tunnel.aigc.zj.cn" })))
      .toBe("https://abc123-3000.tunnel.aigc.zj.cn/");
  });
  it("localhost:port/path → 隧道 URL 保留子路径(path 模式)", () => {
    expect(maybeRewriteToTunnel("localhost:3000/admin", S({})))
      .toBe("https://aigc.zj.cn/relay/t/abc123/3000/admin");
  });
  it("已是 /t/ 隧道 URL 不重复改写", () => {
    expect(maybeRewriteToTunnel("https://aigc.zj.cn/relay/t/abc123/3000/", S({}))).toBeNull();
  });
  it("subdomain 模式:已是子域 URL 不重复改写", () => {
    expect(maybeRewriteToTunnel(
      "https://abc123-3000.tunnel.aigc.zj.cn/",
      S({ relayTunnelMode: "subdomain", relayTunnelBaseDomain: "tunnel.aigc.zj.cn" })
    )).toBeNull();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @pocket-code/app exec vitest run src/components/PreviewTab/tunnelUrl.test.ts`
Expected: FAIL — 模块不存在。

- [ ] **Step 3: 实现 tunnelUrl.ts**

创建 `packages/app/src/components/PreviewTab/tunnelUrl.ts`:

```ts
// ── 隧道预览 URL 拼装(模式感知,从 PreviewTab 抽出以可测) ──
// path 模式:<relayHttpBase>/t/<id>/<port>/  ;  subdomain 模式:<id>-<port>.<baseDomain>/

export interface TunnelInfo {
  mode: "subdomain" | "path";
  baseDomain: string | null;
}

interface TunnelSettings {
  workspaceMode?: string;
  relayServerUrl?: string;
  relayMachineId?: string;
  relayTunnelMode?: "subdomain" | "path";
  relayTunnelBaseDomain?: string | null;
}

function toHttp(wsUrl: string): string {
  return wsUrl.replace(/^ws:\/\//, "http://").replace(/^wss:\/\//, "https://");
}

export function buildTunnelUrl(
  relayServerUrl: string,
  machineId: string,
  port: number,
  info?: TunnelInfo
): string {
  const httpBase = toHttp(relayServerUrl);
  if (info?.mode === "subdomain" && info.baseDomain) {
    const scheme = httpBase.startsWith("https://") ? "https" : "http";
    return `${scheme}://${machineId}-${port}.${info.baseDomain}/`;
  }
  // path 模式(默认 / 回退)
  return `${httpBase.replace(/\/$/, "")}/t/${machineId}/${port}/`;
}

/** 已经是隧道 URL(path 或 subdomain)则不重复改写。 */
function isAlreadyTunnel(input: string, info?: TunnelInfo): boolean {
  if (/\/t\/[^/]+\/\d+/.test(input)) return true;
  if (info?.mode === "subdomain" && info.baseDomain) {
    const esc = info.baseDomain.replace(/[.]/g, "\\.");
    if (new RegExp(`^https?://[0-9a-z]+-\\d+\\.${esc}`, "i").test(input)) return true;
  }
  return false;
}

export function maybeRewriteToTunnel(input: string, settings: TunnelSettings | undefined): string | null {
  if (!settings || settings.workspaceMode !== "relay") return null;
  const { relayServerUrl, relayMachineId } = settings;
  if (!relayServerUrl || !relayMachineId) return null;

  const info: TunnelInfo | undefined = settings.relayTunnelMode
    ? { mode: settings.relayTunnelMode, baseDomain: settings.relayTunnelBaseDomain ?? null }
    : undefined;

  if (isAlreadyTunnel(input, info)) return null;

  // 裸端口 "3000"
  const bare = input.match(/^(\d{1,5})$/);
  if (bare) {
    return buildTunnelUrl(relayServerUrl, relayMachineId, parseInt(bare[1], 10), info);
  }
  // localhost / 127.0.0.1 (+可选 http:// / 端口 / 子路径)
  const loc = input.match(/^(?:https?:\/\/)?(?:localhost|127\.0\.0\.1)(?::(\d{1,5}))?(\/.*)?$/i);
  if (loc) {
    const port = loc[1] ? parseInt(loc[1], 10) : 80;
    const base = buildTunnelUrl(relayServerUrl, relayMachineId, port, info);
    const rest = loc[2] ? loc[2].replace(/^\//, "") : "";
    return base + rest;
  }
  return null;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @pocket-code/app exec vitest run src/components/PreviewTab/tunnelUrl.test.ts`
Expected: PASS。

- [ ] **Step 5: 加 settings 运行时字段**

`packages/app/src/store/settings.ts` 在 `relayMachineId?` 之后加:

```ts
    /** 极客模式:relay 隧道模式(运行时从 /health 拉取,不落盘持久化) */
    relayTunnelMode?: "subdomain" | "path";

    /** 极客模式:relay 子域基础域名(subdomain 模式,运行时拉取) */
    relayTunnelBaseDomain?: string | null;
```

> 这两个是运行时缓存字段,不进 `DEFAULT_SETTINGS`(拉取到才有值),回退逻辑靠 `buildTunnelUrl` 里 `info` 为空走 path。

- [ ] **Step 6: 改 index.tsx 用 tunnelUrl.ts + 拉 /health**

`packages/app/src/components/PreviewTab/index.tsx`:

1. 删除文件内私有的 `buildTunnelUrl`(第 20-24 行)和 `maybeRewriteToTunnel`(第 31-53 行),改为顶部 import:

```ts
import { maybeRewriteToTunnel } from "./tunnelUrl";
```

2. 组件内加一次性 `/health` 拉取,把 mode/baseDomain 合并进传给 `maybeRewriteToTunnel` 的 settings。最小实现:用本地 state 存 tunnelInfo,useEffect 里 fetch:

```ts
  const [tunnelInfo, setTunnelInfo] = useState<{ relayTunnelMode?: "subdomain" | "path"; relayTunnelBaseDomain?: string | null }>({});

  React.useEffect(() => {
    if (!settings || settings.workspaceMode !== "relay" || !settings.relayServerUrl) return;
    const httpBase = settings.relayServerUrl.replace(/^ws:\/\//, "http://").replace(/^wss:\/\//, "https://").replace(/\/relay\/?$/, "");
    let cancelled = false;
    fetch(`${httpBase}/health`)
      .then((r) => r.json())
      .then((j) => { if (!cancelled && j && (j.tunnelMode === "subdomain" || j.tunnelMode === "path")) {
        setTunnelInfo({ relayTunnelMode: j.tunnelMode, relayTunnelBaseDomain: j.tunnelBaseDomain ?? null });
      }})
      .catch(() => { /* 旧 relay / 拉取失败:静默,buildTunnelUrl 回退 path */ });
    return () => { cancelled = true; };
  }, [settings?.relayServerUrl, settings?.workspaceMode]);
```

3. `handleGo` 里调用处把 tunnelInfo 合并进 settings:

```ts
    const tunnel = maybeRewriteToTunnel(target, settings ? { ...settings, ...tunnelInfo } : undefined);
```

> `/health` 路径:relayServerUrl 形如 `wss://aigc.zj.cn/relay`,`/health` 在**根**(`https://aigc.zj.cn/health`),故上面用 `.replace(/\/relay\/?$/, "")` 去掉 `/relay` 再接 `/health`。

- [ ] **Step 7: App 构建/类型检查通过**

Run: `pnpm --filter @pocket-code/app exec vitest run src/components/PreviewTab/ && pnpm typecheck:app`
Expected: PASS(tunnelUrl 测试全绿;App 无 TS 错误)。

> 若 `typecheck:app` 有与本改动无关的既存错误,只需确认没有**新增**错误来自 PreviewTab/tunnelUrl/settings。

- [ ] **Step 8: Commit**

```bash
git add packages/app/src/components/PreviewTab/tunnelUrl.ts packages/app/src/components/PreviewTab/tunnelUrl.test.ts packages/app/src/components/PreviewTab/index.tsx packages/app/src/store/settings.ts
git commit -m "feat(app): PreviewTab 模式感知拼预览 URL(经 /health 取 mode,子域/路径回退)"
```

---

### Task 9: e2e 子域场景 + 文档(部署/迁移/证书)

**Files:**
- Modify: `packages/relay/src/e2e.tunnel.test.ts`(加子域场景)
- Modify: `packages/relay/README.md`
- Modify: `docs/deployment-relay-daemon.md`(若存在;不存在则在 README 内补部署段)

**Interfaces:**
- Consumes: 全部前序任务(端到端验证)。
- Produces: 无代码接口;文档 + 一条 e2e 子域场景。

**背景:** 见 spec「测试策略 #7」「向后兼容与迁移」「部署:Cloudflare DNS-01」。

- [ ] **Step 1: 写子域 e2e 场景(失败)**

在 `packages/relay/src/e2e.tunnel.test.ts` 末尾、最外层 `describe` 内追加场景 C:

```ts
  describe("场景C: 子域模式(TUNNEL_MODE=subdomain)", () => {
    const relayPort = randPort();
    const BASE = "tunnel.localhost";
    const MID = "aa11bb22cc33dd44";
    let relay: ChildProcess;
    let target: { server: Server; port: number };
    let client: TunnelClientHandle;

    beforeAll(async () => {
      relay = spawnRelay({
        PORT: String(relayPort), RELAY_SECRET: SECRET,
        RELAY_DISCOVERY: "off", TUNNEL_TOKEN: "",
        TUNNEL_MODE: "subdomain", TUNNEL_BASE_DOMAIN: BASE,
      });
      await waitHealthy(relayPort);
      target = await startTarget();
      client = startTunnelClient({
        relayUrl: `ws://127.0.0.1:${relayPort}/relay`,
        relaySecret: SECRET, machineId: MID, machineName: "e2e-c",
      });
      await waitMachines(relayPort, 1);
    }, 30000);

    afterAll(() => { client?.stop(); target?.server.close(); relay?.kill(); });

    it("/health 报告 subdomain 模式 + baseDomain", async () => {
      const j = await (await fetch(`http://127.0.0.1:${relayPort}/health`)).json();
      expect(j.tunnelMode).toBe("subdomain");
      expect(j.tunnelBaseDomain).toBe(BASE);
    }, 10000);

    it("Host 驱动路由:<id>-<port>.<base> 经隧道取回目标响应", async () => {
      // relay 从 Host 解析目标;path 直达根,forwardPath=/hello
      const resp = await fetch(`http://127.0.0.1:${relayPort}/hello?x=1`, {
        headers: { host: `${MID}-${target.port}.${BASE}` },
      });
      expect(resp.status).toBe(200);
      expect(await resp.text()).toBe("hello-tunnel:/hello?x=1");
    }, 15000);

    it("主站 Host + 非 /health 路径 → 404(非隧道)", async () => {
      const resp = await fetch(`http://127.0.0.1:${relayPort}/whatever`, { headers: { host: BASE } });
      expect(resp.status).toBe(404);
    }, 10000);
  });
```

- [ ] **Step 2: 运行确认通过(实现已在前序任务完成,此处是端到端验证)**

Run: `pnpm --filter @pocket-code/relay exec vitest run src/e2e.tunnel.test.ts`
Expected: PASS(三场景全绿)。

> 若场景 C 失败,说明前序集成有缺口——回到对应任务修,不在此加补丁。

- [ ] **Step 3: 更新 relay README(两模式 + 环境变量表 + Cloudflare 证书 + 迁移)**

`packages/relay/README.md`:在环境变量表加 `TUNNEL_MODE`、`TUNNEL_BASE_DOMAIN` 两行,并新增两节。表格追加:

```markdown
| `TUNNEL_MODE` | 隧道寻址模式:`subdomain`(默认,完整解法)/ `path`(路径前缀,裸 IP/无泛证书回退) | `subdomain` |
| `TUNNEL_BASE_DOMAIN` | 子域模式必需,如 `tunnel.example.com`;缺失则启动失败 | (无) |
```

新增小节:

```markdown
## 隧道寻址模式

- **subdomain(默认)**:`https://<machineId>-<port>.<TUNNEL_BASE_DOMAIN>/`。应用绝对路径天然正确、
  链接可分享、SPA 客户端路由不掉前缀。需泛域名 + 泛证书。
- **path**(`TUNNEL_MODE=path`):`https://<host>/t/<machineId>/<port>/`。用于裸 IP / 无泛证书部署。
  服务端 3xx 重定向经 relay 补 `/t/<id>/<port>` 前缀不掉出;但 SPA 纯客户端 `pushState`、
  多机器并用会串——完整解请用 subdomain 模式。

> **破坏性变更**:默认模式为 `subdomain`。现有靠 `/t/` 路径的部署升级后**必须显式设 `TUNNEL_MODE=path`**,
> 否则启动会因缺 `TUNNEL_BASE_DOMAIN` 而失败(subdomain 模式硬要求 baseDomain)。

## 子域模式证书(Cloudflare DNS-01 泛证书,推荐)

要覆盖 `*.<TUNNEL_BASE_DOMAIN>`(二级子域通配)。Cloudflare 免费 Universal SSL **不覆盖**二级通配,
推荐让 Cloudflare 只当 DNS 供应商,证书由 Let's Encrypt DNS-01 签(免费,TLS 在自管 nginx 终止):

1. Cloudflare 建 API Token,权限 `Zone.DNS:Edit`。
2. VPS 装 `certbot` + `python3-certbot-dns-cloudflare`,用该 token 签 `*.tunnel.example.com`(+ `tunnel.example.com`)。
3. nginx `server { server_name *.tunnel.example.com; ... }` 反代 relay,证书指向泛证书;certbot 定时续签。
4. 泛 DNS 记录 `*.tunnel.example.com` → relay VPS IP。

备选:Cloudflare 付费 ACM(边缘终止,需橙云代理)。

## machineId 迁移(纯 hex)

machineId 从 `m_<hex>` 改为纯 `<hex>`(DNS-safe)。升级后**已配对设备需重新配对**:
删除 `~/.pocket-code/machine-id`(daemon)与 `~/.pocket-tunnel.json`(pocket-tunnel CLI),重启后自动生成新 id 并重新配对。
```

- [ ] **Step 4: 若存在 docs/deployment-relay-daemon.md,同步 nginx 子域段**

先确认存在:`ls docs/deployment-relay-daemon.md`。若存在,在其 nginx 配置段补 `*.tunnel.<domain>` 的 server 块示例(与 README 一致),并标注两模式各自的 nginx 差异(path 模式只需 `location /relay` + `location /t/`;subdomain 模式需泛域名 server 块)。若不存在,跳过(README 已覆盖)。

- [ ] **Step 5: 全仓测试回归**

Run: `pnpm test:all`
Expected: PASS(全部 10 包)。

> 若 App 或其他包有与本改动无关的既存失败,确认失败集合与改动前一致(无新增),记为环境既存问题,不在本任务修。

- [ ] **Step 6: Commit**

```bash
git add packages/relay/src/e2e.tunnel.test.ts packages/relay/README.md docs/deployment-relay-daemon.md
git commit -m "test(relay): e2e 子域场景 + docs(两模式/Cloudflare DNS-01/machineId 迁移)"
```

---

## 执行顺序与依赖

```
Task 1 (machineId hex) ─┐
Task 2 (config mode)   ─┼→ Task 3 (tunnelAddress) ─┐
                        │                           ├→ Task 6 (HTTP 接线) ─→ Task 7 (WS 接线) ─→ Task 9 (e2e+docs)
Task 4 (redirectRewrite)┘                           │
Task 5 (fetch manual) ──────────────────────────────┘
Task 8 (App URL) 依赖 Task 6 的 /health 字段,可在 Task 6 后任意时点做(与 7/9 无强序)
```

- Task 1、2 无依赖,可最先做(2 是 3 的类型前提)。
- Task 3、4、5 相互独立。
- Task 6 集成 2/3/4,并处理与 e2e 默认模式的交互(Step 11)。
- Task 7 紧接 6(共享 index.ts 的 deps 注入)。
- Task 8 依赖 6 的 /health。
- Task 9 是端到端 + 文档收尾,最后做。
