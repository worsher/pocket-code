# CLI/执行层缺陷修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修执行层"承诺 vs 现实"错配:server 侧补后台进程能力(daemon 级注册表)、CLI 委托路径跨轮记忆(claude --resume / codex-gemini 历史注入)、runCommand 可调超时、runner 空闲超时与 stderr 可见性、prompt 按能力门控。

**Architecture:** 新增 `processRegistry`(daemon 级单例,按 workspace 分组管后台进程)+ 提取 `processKill`(共享 killProcessTree);`nodeBackend` 接线 startProcess/stopProcess;`buildExecTools`/`buildSystemPrompt` 加超时参数与能力门控;`cli/runner` 返回对象化 + 空闲超时 + stderr 尾;claude adapter 加 extractSessionId,`runCliSession` 织入 resume/历史注入。Spec:`docs/superpowers/specs/2026-07-11-cli执行层缺陷修复-design.md`。

**Tech Stack:** TypeScript(ESM,`.js` 导入后缀)、Node child_process、vitest(**不是 jest**)、pnpm workspace。全部改动在 `packages/server`、`packages/agent-core`、`packages/daemon`;agent-core 保持零运行时依赖。

## Global Constraints

- 分支:`feature/cli-exec-fixes`(从 master 切出;执行者若已在此分支则直接工作)。
- **agent-core 零运行时依赖**:agent-core 改动仅限 schema 文本、prompt 拼接、类型 —— 不得 import child_process 或任何 node 运行时进程 API(进程实现在 server 侧 backend)。
- **App 端行为零变化**:deviceBackend 已实现 startProcess;`buildSystemPrompt` 的 `supportsBackground` 默认为 true(保持 App 现状),仅 server 显式按 backend 能力传入。
- **既有 CLI 路径无回归**:resume 内存态丢失(server 重启/TTL)时降级为"全新会话"= 现状,不报错。
- 进程归属:**daemon 级常驻** —— session TTL/断连不杀后台进程;同 workspace + 同 command 再起先杀旧。
- 超时:runCommand 默认 **120s**,可选 `timeoutSeconds` clamp **[1, 600]**;runner **空闲**超时 **120s**(非全局)。
- 错误文案(逐字):runner 空闲超时 → `CLI 无响应,已终止(120s 无输出)`。
- ESM 导入:server/daemon/agent-core 均 `"type":"module"`,相对 import 必须带 `.js` 后缀。
- 验证门:各任务末尾 `pnpm --filter @pocket-code/<pkg> test` 相关包全绿;末任务 `pnpm test:all` EXIT 0。
- 链路验收(relay 起 dev server、claude 两轮记忆、npm install 不超时)为**后置项**,非任务门禁 —— 报告注明未做即可。
- 提交信息中文,`feat:`/`refactor:`/`docs:` 前缀,结尾带:

```
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01VTWQvYMuNm4MxVzL3QQyK5
```

## File Structure(全景)

| 文件 | 动作 | 任务 |
|---|---|---|
| `packages/server/src/processKill.ts` + `.test.ts` | 建(从 runner 提 killProcessTree) | T1 |
| `packages/server/src/cli/runner.ts` | 改(import 提取的 killProcessTree) | T1 |
| `packages/server/src/processRegistry.ts` + `.test.ts` | 建(daemon 级注册表) | T2 |
| `packages/server/src/nodeBackend.ts` + `.test.ts` | 改(startProcess/stopProcess 接线) | T3 |
| `packages/server/src/index.ts` | 改(挂 SIGTERM/SIGINT/exit → shutdownAll) | T3 |
| `packages/daemon/src/index.ts` | 改(shutdown() 内加 shutdownAll) | T3 |
| `packages/agent-core/src/tools/execTools.ts` + `.test.ts` | 改(runCommand timeoutSeconds) | T4 |
| `packages/agent-core/src/prompt.ts` + `.test.ts` | 改(supportsBackground 门控) | T5 |
| `packages/server/src/agent.ts` | 改(buildSystemPrompt 传 supportsBackground) | T5 |
| `packages/server/src/cli/runner.ts` + `.test.ts` | 改(返回对象+空闲超时+stderr 尾+extractSessionId 采集) | T6 |
| `packages/server/src/cli/types.ts` | 改(resumeSessionId/extractSessionId 契约) | T6 |
| `packages/server/src/cli/claudeCode.ts` + `.test.ts` | 改(extractSessionId + --resume) | T7 |
| `packages/server/src/agent.ts` (AgentSession) + `cli/index.ts` (runCliSession) + `.test.ts` | 改(cliSessions 存取 + 历史注入) | T7 |
| `packages/server/README`/`docs` + `plan.md` | 改(已知限制 + 拆分路线状态) | T8 |

---

### Task 1: 提取 killProcessTree 为共享模块

**Files:**
- Create: `packages/server/src/processKill.ts`
- Create: `packages/server/src/processKill.test.ts`
- Modify: `packages/server/src/cli/runner.ts`(现 L12-49 的 `isProcessAlive`/`killProcessTree` 删除,改 import)

**Interfaces:**
- Produces:`killProcessTree(pid: number, graceMs?: number): void`、`isProcessAlive(pid: number): boolean` —— T2 registry 与 runner 共用。

**背景(零上下文实现者):** `cli/runner.ts:12-49` 现有 `isProcessAlive` + `killProcessTree`(跨平台进程树终止:SIGTERM→grace→SIGKILL,用进程组信号 `-pid`;win32 走 taskkill)。T2 的进程注册表也要杀进程,故提取共享。**纯搬运,逻辑一字不改**。

- [ ] **Step 1: 写测试 `processKill.test.ts`(注入假 kill,断言信号序列)**

```ts
import { afterEach, describe, expect, it, vi } from "vitest";

describe("killProcessTree (posix)", () => {
  afterEach(() => vi.restoreAllMocks());

  it("先对进程组发 SIGTERM", async () => {
    if (process.platform === "win32") return; // posix-only 断言
    const { killProcessTree } = await import("./processKill.js");
    const killSpy = vi.spyOn(process, "kill").mockImplementation(() => true);
    killProcessTree(12345, 3000);
    expect(killSpy).toHaveBeenCalledWith(-12345, "SIGTERM");
  });

  it("pid 非法直接返回不抛", async () => {
    const { killProcessTree } = await import("./processKill.js");
    expect(() => killProcessTree(0)).not.toThrow();
    expect(() => killProcessTree(-5)).not.toThrow();
  });
});

describe("isProcessAlive", () => {
  it("对存活进程(自身)返回 true", async () => {
    const { isProcessAlive } = await import("./processKill.js");
    expect(isProcessAlive(process.pid)).toBe(true);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/server test -- processKill`
Expected: FAIL — `Cannot find module './processKill.js'`。

- [ ] **Step 3: 建 `processKill.ts`(从 runner.ts 原样搬 L12-49 两函数 + export)**

```ts
// 跨平台进程树终止(从 cli/runner.ts 提取,供 runner 与 processRegistry 共用)。
// 先 SIGTERM,grace 后 SIGKILL(用进程组信号 -pid);win32 走 taskkill /T。
import { spawn as nodeSpawn } from "node:child_process";

export function isProcessAlive(pid: number): boolean {
  try {
    process.kill(pid, 0);
    return true;
  } catch {
    return false;
  }
}

export function killProcessTree(pid: number, graceMs = 3000): void {
  if (!Number.isFinite(pid) || pid <= 0) return;
  if (process.platform === "win32") {
    try {
      nodeSpawn("taskkill", ["/T", "/PID", String(pid)], { stdio: "ignore", detached: true });
    } catch { /* ignore */ }
    setTimeout(() => {
      if (!isProcessAlive(pid)) return;
      try {
        nodeSpawn("taskkill", ["/F", "/T", "/PID", String(pid)], { stdio: "ignore", detached: true });
      } catch { /* ignore */ }
    }, graceMs).unref();
    return;
  }
  try {
    process.kill(-pid, "SIGTERM");
  } catch {
    try { process.kill(pid, "SIGTERM"); } catch { return; }
  }
  setTimeout(() => {
    if (isProcessAlive(-pid)) {
      try { process.kill(-pid, "SIGKILL"); return; } catch { /* fall through */ }
    }
    if (isProcessAlive(pid)) {
      try { process.kill(pid, "SIGKILL"); } catch { /* already gone */ }
    }
  }, graceMs).unref();
}
```

- [ ] **Step 4: 改 `cli/runner.ts` 用 import 替代本地定义**

删除 runner.ts 的 `isProcessAlive`(L12-19)与 `killProcessTree`(L21-49);在 import 区(L6-8 附近)加:

```ts
import { killProcessTree } from "../processKill.js";
```

(runner 只用 `killProcessTree`;`isProcessAlive` 若 runner 内无其他引用则不 import。)

- [ ] **Step 5: 跑测试确认通过 + 回归 runner**

Run: `pnpm --filter @pocket-code/server test -- processKill && pnpm --filter @pocket-code/server test -- runner`
Expected: processKill 全 PASS;runner 既有测试全 PASS(killProcessTree 行为未变)。

- [ ] **Step 6: Commit**

```bash
git add packages/server/src/processKill.ts packages/server/src/processKill.test.ts packages/server/src/cli/runner.ts
git commit -m "refactor(server): 提取 killProcessTree 为共享 processKill 模块(供 runner 与 processRegistry 共用)"
```

---

### Task 2: processRegistry — daemon 级后台进程注册表

**Files:**
- Create: `packages/server/src/processRegistry.ts`
- Create: `packages/server/src/processRegistry.test.ts`

**Interfaces:**
- Consumes: T1 `killProcessTree`;`docker.ts` 的 `isDockerEnabled(): boolean`(现有);docker 起进程用 `child_process.spawn`(不复用 execInContainer —— 那是一次性 exec)。
- Produces(T3 依赖,签名逐字):

```ts
export interface ManagedProcess { processId: string; workspace: string; command: string; pid: number; containerId?: string; startedAt: number; }
export function startManaged(workspace: string, command: string, opts?: { containerId?: string }): Promise<{ processId: string }>
export function stopManaged(processId: string): Promise<void>
export function listManaged(workspace?: string): ManagedProcess[]
export function shutdownAll(): void
// 测试注入点(默认用真实 child_process.spawn):
export function __setSpawnForTest(fn: typeof import("node:child_process").spawn | null): void
```

**背景:** daemon 级单例(模块级 `Map<processId, ManagedProcess>`)。host 分支 `spawn(command, { shell:true, detached:true, stdio:"ignore" })` 后 `proc.unref()`,记 `proc.pid`;进程 `close` 事件自动从 Map 删。docker 分支 `spawn("docker", ["exec","-d",containerId,"sh","-c",command])`——`-d` 拿不到容器内 pid,故 docker 的 ManagedProcess.pid 记宿主 docker 客户端 pid(仅占位),`stopManaged` 对 docker 项走容器内 `pkill`(见实现)。同 workspace+同 command 再起先 stop 旧。processId 用 `crypto.randomBytes(6).toString("hex")` 前缀 `p_`。

- [ ] **Step 1: 写测试 `processRegistry.test.ts`**

```ts
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import { EventEmitter } from "node:events";
import { startManaged, stopManaged, listManaged, shutdownAll, __setSpawnForTest } from "./processRegistry.js";

// 假子进程:带 pid、可发 close、记录 kill 调用
class FakeChild extends EventEmitter {
  pid = Math.floor(Math.random() * 90000) + 1000;
  unref = vi.fn();
  kill = vi.fn();
}

let spawned: { cmd: string; args: readonly string[]; child: FakeChild }[] = [];
function fakeSpawn(cmd: string, args?: readonly string[]): any {
  const child = new FakeChild();
  spawned.push({ cmd, args: args ?? [], child });
  return child;
}

beforeEach(() => { spawned = []; __setSpawnForTest(fakeSpawn as any); shutdownAll(); });
afterEach(() => { __setSpawnForTest(null); vi.restoreAllMocks(); });

describe("processRegistry (host mode)", () => {
  it("startManaged 返回 p_ 前缀 id 并登记", async () => {
    const { processId } = await startManaged("/ws/a", "npm run dev");
    expect(processId).toMatch(/^p_[0-9a-f]+$/);
    const list = listManaged("/ws/a");
    expect(list).toHaveLength(1);
    expect(list[0]).toMatchObject({ processId, workspace: "/ws/a", command: "npm run dev" });
  });

  it("shell 模式起进程且 unref", async () => {
    await startManaged("/ws/a", "vite");
    expect(spawned[0].cmd).toBe("vite");
    expect(spawned[0].child.unref).toHaveBeenCalled();
  });

  it("同 workspace+同 command 再起先杀旧", async () => {
    const first = await startManaged("/ws/a", "npm run dev");
    const firstChild = spawned[0].child;
    await startManaged("/ws/a", "npm run dev");
    expect(firstChild.kill).toHaveBeenCalled();       // 旧的被 stop
    expect(listManaged("/ws/a")).toHaveLength(1);      // 不堆积
    expect(listManaged("/ws/a")[0].processId).not.toBe(first.processId);
  });

  it("进程自然退出自动摘除", async () => {
    await startManaged("/ws/a", "server");
    spawned[0].child.emit("close", 0);
    expect(listManaged("/ws/a")).toHaveLength(0);
  });

  it("listManaged 按 workspace 过滤;无参返回全部", async () => {
    await startManaged("/ws/a", "a");
    await startManaged("/ws/b", "b");
    expect(listManaged("/ws/a")).toHaveLength(1);
    expect(listManaged()).toHaveLength(2);
  });

  it("stopManaged 未知 id 安全无操作", async () => {
    await expect(stopManaged("p_nope")).resolves.toBeUndefined();
  });

  it("shutdownAll 清空全部", async () => {
    await startManaged("/ws/a", "a");
    await startManaged("/ws/b", "b");
    shutdownAll();
    expect(listManaged()).toHaveLength(0);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/server test -- processRegistry`
Expected: FAIL — `Cannot find module './processRegistry.js'`。

- [ ] **Step 3: 实现 `processRegistry.ts`**

```ts
// daemon 级后台进程注册表(单例)。按 workspace 分组管 startProcess 起的长驻进程。
// session TTL/断连不杀;同 workspace+同 command 再起先杀旧;daemon 退出 shutdownAll。
import { spawn as realSpawn } from "node:child_process";
import { randomBytes } from "node:crypto";
import { killProcessTree } from "./processKill.js";
import { isDockerEnabled } from "./docker.js";

export interface ManagedProcess {
  processId: string;
  workspace: string;
  command: string;
  pid: number;
  containerId?: string;
  startedAt: number;
}

type SpawnFn = typeof realSpawn;
let spawnFn: SpawnFn = realSpawn;
export function __setSpawnForTest(fn: SpawnFn | null): void {
  spawnFn = fn ?? realSpawn;
}

const registry = new Map<string, ManagedProcess>();

function newId(): string {
  return "p_" + randomBytes(6).toString("hex");
}

export async function startManaged(
  workspace: string,
  command: string,
  opts?: { containerId?: string }
): Promise<{ processId: string }> {
  // 同 workspace+同 command 先杀旧(防堆积)
  for (const p of listManaged(workspace)) {
    if (p.command === command) await stopManaged(p.processId);
  }

  const processId = newId();
  const containerId = opts?.containerId;

  let child;
  if (containerId && isDockerEnabled()) {
    child = spawnFn("docker", ["exec", "-d", containerId, "sh", "-c", command], { stdio: "ignore" });
  } else {
    child = spawnFn(command, [], { shell: true, detached: true, stdio: "ignore" });
  }
  child.unref?.();

  const rec: ManagedProcess = {
    processId,
    workspace,
    command,
    pid: child.pid ?? -1,
    containerId,
    startedAt: Date.now(),
  };
  registry.set(processId, rec);

  child.on?.("close", () => { registry.delete(processId); });
  child.on?.("error", () => { registry.delete(processId); });

  return { processId };
}

export async function stopManaged(processId: string): Promise<void> {
  const rec = registry.get(processId);
  if (!rec) return;
  registry.delete(processId);
  if (rec.containerId && isDockerEnabled()) {
    // docker exec -d 拿不到容器内 pid,用容器内 pkill 按命令行匹配终止(docker 分支既定简化)
    try { spawnFn("docker", ["exec", rec.containerId, "pkill", "-f", rec.command], { stdio: "ignore" }); }
    catch { /* ignore */ }
    return;
  }
  if (rec.pid > 0) killProcessTree(rec.pid);
}

export function listManaged(workspace?: string): ManagedProcess[] {
  const all = [...registry.values()];
  return workspace ? all.filter((p) => p.workspace === workspace) : all;
}

export function shutdownAll(): void {
  for (const id of [...registry.keys()]) {
    // 同步尽力终止:不 await(退出钩子里)
    void stopManaged(id);
  }
  registry.clear();
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm --filter @pocket-code/server test -- processRegistry`
Expected: 全 PASS(7 例)。

- [ ] **Step 5: Commit**

```bash
git add packages/server/src/processRegistry.ts packages/server/src/processRegistry.test.ts
git commit -m "feat(server): daemon 级后台进程注册表(按 workspace 分组/同命令先杀旧/docker 分支/shutdownAll,离线预览 T2)"
```

---

### Task 3: nodeBackend 接线 startProcess/stopProcess + 入口挂 shutdownAll

**Files:**
- Modify: `packages/server/src/nodeBackend.ts`(`createNodeBackend` 现 L133;RuntimeBackend 返回对象加两方法)
- Modify: `packages/server/src/nodeBackend.test.ts`(补断言)
- Modify: `packages/server/src/index.ts`(挂进程信号)
- Modify: `packages/daemon/src/index.ts`(shutdown() 内加 shutdownAll)

**Interfaces:**
- Consumes: T2 `startManaged`/`stopManaged`/`shutdownAll`、`resolveHostCwd`(nodeBackend.ts 现有内部函数)。
- Produces:`createNodeBackend` 返回的 backend 现在 `startProcess`/`stopProcess` 非 undefined —— 使 `execTools.ts:438` 能力门控通过,runInBackground/stopProcess 工具得以注册。

**背景:** `agent-core/src/types.ts:40-41` 契约里 startProcess/stopProcess 为可选;当前 nodeBackend 未实现,故 server 侧后台工具从不注册(#1 根因)。本任务接线,并把注册表的退出清理挂到两个进程入口(server 无信号钩子需新增;daemon 已有 shutdown() 只需插一行)。

- [ ] **Step 1: nodeBackend 测试补断言**

在 `packages/server/src/nodeBackend.test.ts` 末尾加(mock processRegistry):

```ts
import { vi } from "vitest";
vi.mock("./processRegistry.js", () => ({
  startManaged: vi.fn(async (ws: string, cmd: string) => ({ processId: "p_test" })),
  stopManaged: vi.fn(async () => {}),
}));

describe("nodeBackend 后台进程", () => {
  it("startProcess 转发 startManaged(cwd 经 resolveHostCwd)", async () => {
    const reg = await import("./processRegistry.js");
    const { createNodeBackend } = await import("./nodeBackend.js");
    const be = createNodeBackend("/ws", undefined);
    const r = await be.startProcess!("npm run dev", { cwd: "sub" });
    expect(r.processId).toBe("p_test");
    // resolveHostCwd("/ws","sub") → "/ws/sub" 作为 workspace 传入
    expect(reg.startManaged).toHaveBeenCalledWith("/ws/sub", "npm run dev", { containerId: undefined });
  });

  it("stopProcess 转发 stopManaged", async () => {
    const reg = await import("./processRegistry.js");
    const { createNodeBackend } = await import("./nodeBackend.js");
    const be = createNodeBackend("/ws", undefined);
    await be.stopProcess!("p_x");
    expect(reg.stopManaged).toHaveBeenCalledWith("p_x");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/server test -- nodeBackend`
Expected: FAIL — backend.startProcess is undefined / not a function。

- [ ] **Step 3: nodeBackend 实现两方法**

`packages/server/src/nodeBackend.ts` 顶部 import 加:

```ts
import { startManaged, stopManaged } from "./processRegistry.js";
```

`createNodeBackend`(现 L133 起,返回的对象里)加两方法(startProcess 用 `resolveHostCwd(workspace, opts?.cwd)` 定位,containerId 透传给 registry 走 docker 分支):

```ts
    async startProcess(cmd: string, opts?: { cwd?: string }) {
      const ws = resolveHostCwd(workspace, opts?.cwd);
      return startManaged(ws, cmd, { containerId });
    },
    async stopProcess(processId: string) {
      await stopManaged(processId);
    },
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm --filter @pocket-code/server test -- nodeBackend`
Expected: 两新例 PASS,既有 PASS。

- [ ] **Step 5: server index.ts 挂退出钩子**

`packages/server/src/index.ts` 顶部 import 加:

```ts
import { shutdownAll } from "./processRegistry.js";
```

在 `httpServer.listen(...)` 之后(文件末尾)加:

```ts
// 进程退出时清理所有后台进程(daemon 级常驻,但进程本体退出必须收尾)
let shuttingDown = false;
function onExit() {
  if (shuttingDown) return;
  shuttingDown = true;
  shutdownAll();
}
process.on("SIGTERM", () => { onExit(); process.exit(0); });
process.on("SIGINT", () => { onExit(); process.exit(0); });
process.on("exit", onExit);
```

- [ ] **Step 6: daemon index.ts 的 shutdown() 加 shutdownAll**

`packages/daemon/src/index.ts` 的 import 区加:

```ts
import { shutdownAll } from "@pocket-code/server/processRegistry";
```

(先验证该子路径导出可用:`packages/server/package.json` 的 `exports` 是否已暴露 `./processRegistry` —— 若无,加一条 `"./processRegistry": "./dist/processRegistry.js"`,与既有 `./messageHandler` 同款。)

`shutdown()`(现 L292-296)改为:

```ts
function shutdown() {
  console.log("\n[Daemon] Shutting down...");
  shutdownAll();
  connection.stop();
  process.exit(0);
}
```

- [ ] **Step 7: 全验证 + Commit**

```bash
cd packages/server && npx tsc --noEmit && cd ../..
cd packages/daemon && npx tsc --noEmit && cd ../..
pnpm --filter @pocket-code/server test
git add packages/server/src/nodeBackend.ts packages/server/src/nodeBackend.test.ts packages/server/src/index.ts packages/daemon/src/index.ts packages/server/package.json
git commit -m "feat(server): nodeBackend 接线 startProcess/stopProcess + server/daemon 入口挂 shutdownAll"
```
Expected: 两包 tsc 0 错误;server 测试全绿。

---

### Task 4: runCommand 可调超时

**Files:**
- Modify: `packages/agent-core/src/tools/execTools.ts`(runCommandSchema 现 L47-58;runCommandTool.execute 现 L60 起,`timeoutMs: 30000`)
- Modify: `packages/agent-core/src/tools/execTools.test.ts`(补断言)

**Interfaces:**
- Consumes: 无。
- Produces: runCommand 接受可选 `timeoutSeconds`;默认 120s。

**背景:** 现 runCommand 硬编码 `timeoutMs: 30000`(execTools.ts:66),npm install 等合法长命令被杀,且模型无法调整。agent-core 零依赖:只改 schema 文本与传给 backend.exec 的 timeoutMs 数值,不碰进程 API。

- [ ] **Step 1: 写测试(假 backend 记录 exec opts)**

在 `packages/agent-core/src/tools/execTools.test.ts` 加(沿用文件既有的假 backend 模式;若无,按下例建最小假 backend):

```ts
import { describe, expect, it } from "vitest";
import { buildExecTools } from "./execTools.js";
import type { RuntimeBackend, ExecResult } from "../types.js";

function fakeBackend(capture: { opts?: any }): RuntimeBackend {
  return {
    readFile: async () => "",
    writeFile: async () => ({ isNew: true }),
    listFiles: async () => [],
    async exec(_cmd: string, opts?: any): Promise<ExecResult> {
      capture.opts = opts;
      return { stdout: "ok", stderr: "", exitCode: 0 };
    },
  };
}

function runCmd(tools: ReturnType<typeof buildExecTools>) {
  return tools.find((t) => t.schema.name === "runCommand")!;
}

describe("runCommand timeoutSeconds", () => {
  it("默认 120s", async () => {
    const cap: { opts?: any } = {};
    await runCmd(buildExecTools("/ws")).execute(fakeBackend(cap), { command: "echo hi" });
    expect(cap.opts.timeoutMs).toBe(120000);
  });
  it("模型传参生效", async () => {
    const cap: { opts?: any } = {};
    await runCmd(buildExecTools("/ws")).execute(fakeBackend(cap), { command: "npm i", timeoutSeconds: 300 });
    expect(cap.opts.timeoutMs).toBe(300000);
  });
  it("clamp 上限 600", async () => {
    const cap: { opts?: any } = {};
    await runCmd(buildExecTools("/ws")).execute(fakeBackend(cap), { command: "x", timeoutSeconds: 9999 });
    expect(cap.opts.timeoutMs).toBe(600000);
  });
  it("clamp 下限 1;非法值回默认", async () => {
    const cap: { opts?: any } = {};
    await runCmd(buildExecTools("/ws")).execute(fakeBackend(cap), { command: "x", timeoutSeconds: 0 });
    expect(cap.opts.timeoutMs).toBe(1000);
    const cap2: { opts?: any } = {};
    await runCmd(buildExecTools("/ws")).execute(fakeBackend(cap2), { command: "x", timeoutSeconds: "abc" as any });
    expect(cap2.opts.timeoutMs).toBe(120000);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/agent-core test -- execTools`
Expected: FAIL — 默认仍 30000。

- [ ] **Step 3: 改 execTools.ts**

runCommandSchema 的 properties 加(现 L54-56 的 command 之后):

```ts
        command: { type: "string", description: "The shell command to execute" },
        timeoutSeconds: { type: "number", description: "Command timeout in seconds (default 120, max 600)." },
```

runCommandTool.execute 里 `timeoutMs: 30000`(现 L66)改为按参数 clamp:

```ts
      const rawT = Number((args as any).timeoutSeconds);
      const timeoutMs = (Number.isFinite(rawT) && rawT > 0 ? Math.min(Math.max(Math.floor(rawT), 1), 600) : 120) * 1000;
      const r = await backend.exec(command, {
        cwd: workspace,
        timeoutMs,
        env: GIT_ENV,
        isolateHome: true,
      });
```

- [ ] **Step 4: 跑测试确认通过 + Commit**

Run: `pnpm --filter @pocket-code/agent-core test -- execTools`
Expected: 全 PASS。

```bash
git add packages/agent-core/src/tools/execTools.ts packages/agent-core/src/tools/execTools.test.ts
git commit -m "feat(agent-core): runCommand 可调超时(默认 120s,timeoutSeconds clamp 1..600)"
```

---

### Task 5: prompt 按后台能力门控

**Files:**
- Modify: `packages/agent-core/src/prompt.ts`(`buildSystemPrompt` 签名 + Shell 段拼接;现 L17 Shell 行、L27-28 两 guideline)
- Modify: `packages/agent-core/src/prompt.test.ts`(补两态断言;无则新建)
- Modify: `packages/server/src/agent.ts`(现 L191 `buildSystemPrompt({ customPrompt })` 传 supportsBackground)

**Interfaces:**
- Consumes: 无。
- Produces:`buildSystemPrompt(opts?: { customPrompt?: string; supportsBackground?: boolean }): string` —— `supportsBackground` 缺省为 true(App 端保持现状)。

**背景:** `buildSystemPrompt` 已是对象参形态(agent.ts:191 传 `{ customPrompt }`)。现无条件宣传 runInBackground(prompt.ts:17,27-28),但 server 侧 backend 若无 startProcess 则该工具不存在 → prompt 撒谎(#9)。T3 之后 server backend 已有 startProcess,但门控仍应存在以保正确性(未来其他 backend 可能无此能力)。

- [ ] **Step 1: 先读 prompt.ts 确认 buildSystemPrompt 现有签名**

Run: `grep -n "export function buildSystemPrompt\|customPrompt" packages/agent-core/src/prompt.ts`
(以实际签名为准接改;下方假定现签名为 `buildSystemPrompt(opts?: { customPrompt?: string })`——若为位置参 `buildSystemPrompt(customPrompt?: string)`,则本任务同时把它改为对象参并更新 agent.ts 与 useAgent 的调用点,grep `buildSystemPrompt(` 全仓核对。)

- [ ] **Step 2: 写测试 `prompt.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { buildSystemPrompt } from "./prompt.js";

describe("buildSystemPrompt 后台能力门控", () => {
  it("supportsBackground=true 时含 runInBackground", () => {
    const p = buildSystemPrompt({ supportsBackground: true });
    expect(p).toContain("runInBackground");
  });
  it("supportsBackground=false 时不含 runInBackground/stopProcess", () => {
    const p = buildSystemPrompt({ supportsBackground: false });
    expect(p).not.toContain("runInBackground");
    expect(p).not.toContain("stopProcess");
  });
  it("默认(不传)含 runInBackground(保持 App 现状)", () => {
    const p = buildSystemPrompt({});
    expect(p).toContain("runInBackground");
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/agent-core test -- prompt`
Expected: FAIL — supportsBackground=false 用例失败(现无条件含 runInBackground)。

- [ ] **Step 4: 改 prompt.ts**

签名加 `supportsBackground`(缺省 true);Shell 段与两条 guideline 按开关拼接。把现 L17 的 Shell 行与 L27-28 两 guideline 改为条件拼接 —— 有后台能力时用现文案,无时 Shell 行只列 runCommand 且删两条 runInBackground guideline:

```ts
export function buildSystemPrompt(opts?: { customPrompt?: string; supportsBackground?: boolean }): string {
  const customPrompt = opts?.customPrompt;
  const supportsBackground = opts?.supportsBackground ?? true;

  const shellLine = supportsBackground
    ? "- Shell: runCommand (one-shot commands that exit), runInBackground (long-running servers/watchers), stopProcess (stop a background process)"
    : "- Shell: runCommand (one-shot commands that exit)";

  const backgroundGuidelines = supportsBackground
    ? `
- NEVER run long-running server/watcher commands via runCommand — use runInBackground instead. Examples: npm run dev, npm start, vite, nodemon, python -m http.server, webpack --watch.
- After starting a dev server with runInBackground, tell the user the port (e.g. http://localhost:5173) so they can open it in the browser. They can stop it with stopProcess.`
    : "";

  let prompt = `You are Pocket Code, an AI coding assistant running on a mobile device. You help developers write, debug, and manage code through natural conversation.

You have access to a workspace directory where you can read/write files and execute commands. Use the tools provided to help the user.

Available tool categories:
- File operations: readFile, writeFile, listFiles
- Git: gitClone, gitStatus, gitAdd, gitCommit, gitPush, gitPull, gitLog, gitBranch, gitCheckout
${shellLine}

Guidelines:
- Be concise in your responses (mobile screen is small)
- When modifying files, always read them first to understand the context
- After making changes, verify by reading the file or running relevant commands
- Use markdown for code blocks with language tags
- When executing commands, explain what you're doing briefly
- If a command fails, try to diagnose and fix the issue
- ALWAYS use the dedicated git tools (gitClone, gitCommit, etc.) instead of runCommand for git operations${backgroundGuidelines}
- IMPORTANT: The workspace root is NOT a git repository. When you clone a repo (e.g. gitClone with url "https://gitee.com/user/my-repo"), it creates a subdirectory (e.g. "my-repo"). All subsequent git operations (gitStatus, gitAdd, gitCommit, gitPush, etc.) MUST pass the repo directory name as the "path" parameter (e.g. path: "my-repo").`;

  if (customPrompt?.trim()) {
    prompt += `\n\n## Project Instructions\n${customPrompt.trim()}`;
  }
  return prompt;
}
```

(以文件实际 prompt 原文为准逐字保留其余行 —— 只在 shellLine/backgroundGuidelines 两处做开关;若现有文案与上方有出入,保留现有文案,仅把 runInBackground 相关行抽进条件分支。)

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm --filter @pocket-code/agent-core test -- prompt`
Expected: 3 例 PASS。

- [ ] **Step 6: agent.ts 传 supportsBackground**

`packages/server/src/agent.ts:191` 的 `buildSystemPrompt({ customPrompt: session.customPrompt })` 改为:

```ts
      system: buildSystemPrompt({
        customPrompt: session.customPrompt,
        supportsBackground: !!createNodeBackend(session.workspace, session.containerId).startProcess,
      }),
```

(createNodeBackend 已在该行下方 L189 调用一次;为避免重复构造,可提取:在 try 前 `const backend = createNodeBackend(session.workspace, session.containerId);` 然后 L189 与此处都用 `backend`。以最小改动为准,若提取则同步 L189。)

- [ ] **Step 7: 全验证 + Commit**

```bash
cd packages/agent-core && npx tsc --noEmit && cd ../..
cd packages/server && npx tsc --noEmit && cd ../..
pnpm --filter @pocket-code/agent-core test && pnpm --filter @pocket-code/server test
git add packages/agent-core/src/prompt.ts packages/agent-core/src/prompt.test.ts packages/server/src/agent.ts
git commit -m "feat(agent-core): buildSystemPrompt 按 supportsBackground 门控 runInBackground 宣传(消除 prompt 承诺不存在的能力)"
```
Expected: 两包 tsc 0 错误、测试全绿。

---

### Task 6: runner 返回对象化 + 空闲超时 + stderr 尾 + session_id 采集

**Files:**
- Modify: `packages/server/src/cli/runner.ts`(`runCliAgent` 现 L55-144)
- Modify: `packages/server/src/cli/runner.test.ts`(补断言)
- Modify: `packages/server/src/cli/types.ts`(`CliSpawnContext` 加 resumeSessionId;`CliAgentAdapter` 加 extractSessionId)

**Interfaces:**
- Consumes: T1 `killProcessTree`。
- Produces(T7 依赖,签名逐字):
  - `runCliAgent(...)` 返回类型从 `Promise<string>` 改为 `Promise<{ fullText: string; cliSessionId?: string }>`。
  - `CliSpawnContext` 加 `resumeSessionId?: string`。
  - `CliAgentAdapter` 加 `extractSessionId?(line: string): string | undefined`。
  - 空闲超时常量 `IDLE_TIMEOUT_MS = 120000`。

**背景(已核对 runner.ts 实况):** runner 现 `Promise<string>`,**两处** `resolve(fullText)`:close 回调(L130)、error 回调(L141);`new Promise<string>` 在 L110。改为对象带可选 cliSessionId(claude 的 session_id,逐行经 `adapter.extractSessionId` 采集)。`handle` 里 error 事件**已有** `console.error`(L100);**stderr** 才是仅 `console.warn`(L118-121)——F 块只改这段。加空闲超时(每次 stdout/stderr data 重置 120s 计时器)。**关键**:空闲超时触发时 `killProcessTree(pid)` 会引发 proc 的 close 事件 → 现有 close 回调自然 resolve,**不要**在超时回调里再 `resolve`(会与 close 双重 resolve;Promise 幂等但语义混乱);超时回调只 `handle(error 事件)` + `killProcessTree`,让 close 收尾。

- [ ] **Step 1: types.ts 扩契约**

`CliSpawnContext` 加字段:

```ts
export interface CliSpawnContext {
  workspace: string;
  customPrompt?: string;
  /** claude 续接:上轮捕获的 session_id(有则 buildSpawn 加 --resume)。 */
  resumeSessionId?: string;
}
```

`CliAgentAdapter` 加可选方法:

```ts
  /** 可选:从 stdout 一行提取底层 CLI 的 session_id(claude stream-json init 消息)。首次命中即被 runner 记住。 */
  extractSessionId?(line: string): string | undefined;
```

- [ ] **Step 2: 写 runner 测试(假 spawn 驱动)**

在 `packages/server/src/cli/runner.test.ts` 加(沿用文件既有假 spawn/EventEmitter 模式):

```ts
// 若文件已有 makeFakeProc/fakeSpawn 辅助则复用;以下假定其存在或按此建。
describe("runCliAgent 返回对象 + session_id 采集", () => {
  it("返回 { fullText, cliSessionId }", async () => {
    const adapter = {
      id: "claude-code" as const, supportsResume: true,
      buildSpawn: () => ({ cmd: "claude", args: [], env: {} as any, cwd: "/ws" }),
      parseLine: (l: string) => (l.includes("hello") ? [{ type: "text-delta", text: "hello" } as any] : []),
      extractSessionId: (l: string) => { try { const m = JSON.parse(l); return m.session_id; } catch { return undefined; } },
    };
    const events: any[] = [];
    const { proc, spawnFn } = makeFakeSpawn();
    const p = runCliAgent(adapter as any, "hi", { workspace: "/ws" }, (e) => events.push(e), undefined, spawnFn);
    proc.stdout.emit("data", Buffer.from('{"session_id":"sess_123"}\n'));
    proc.stdout.emit("data", Buffer.from("hello\n"));
    proc.emit("close", 0);
    const result = await p;
    expect(result.fullText).toBe("hello");
    expect(result.cliSessionId).toBe("sess_123");
  });
});

describe("runCliAgent 空闲超时", () => {
  it("120s 无输出 → kill + error 事件", async () => {
    vi.useFakeTimers();
    const killSpy = vi.spyOn(process, "kill").mockImplementation(() => true);
    const adapter = { id: "codex" as const, supportsResume: false,
      buildSpawn: () => ({ cmd: "codex", args: [], env: {} as any, cwd: "/ws" }), parseLine: () => [] };
    const events: any[] = [];
    const { proc, spawnFn } = makeFakeSpawn();
    const p = runCliAgent(adapter as any, "hi", { workspace: "/ws" }, (e) => events.push(e), undefined, spawnFn);
    await vi.advanceTimersByTimeAsync(120001);
    proc.emit("close", null);
    await p;
    expect(events.some((e) => e.type === "error" && e.message.includes("120s 无输出"))).toBe(true);
    vi.useRealTimers();
  });
});
```

(`makeFakeSpawn` — 若 runner.test.ts 尚无,加一个返回 `{ proc, spawnFn }` 的 helper:proc 是带 stdout/stderr EventEmitter + pid + kill 的 EventEmitter,spawnFn 返回它。)

- [ ] **Step 3: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/server test -- runner`
Expected: FAIL — 返回值是 string 无 .fullText / 无空闲超时。

- [ ] **Step 4: 改 runner.ts**

① 返回类型与常量:

```ts
const IDLE_TIMEOUT_MS = 120000;
```

`runCliAgent` 签名返回改 `Promise<{ fullText: string; cliSessionId?: string }>`。

② 采集 session_id:在 `let fullText = ""` 附近加 `let cliSessionId: string | undefined;`;在 `drainLine`/`handle` 逐行处,先试 `if (!cliSessionId && adapter.extractSessionId) { const sid = adapter.extractSessionId(line); if (sid) cliSessionId = sid; }`(放在 drainLine 里对每行调)。

③ 空闲超时(超时回调**只** kill + 发 error,不 resolve —— kill 触发的 close 回调负责 resolve):

```ts
    let idleTimer: NodeJS.Timeout | undefined;
    const resetIdle = () => {
      if (idleTimer) clearTimeout(idleTimer);
      idleTimer = setTimeout(() => {
        handle({ type: "error", message: "CLI 无响应,已终止(120s 无输出)" });
        if (proc.pid) killProcessTree(proc.pid); // → 触发 close → resolve
      }, IDLE_TIMEOUT_MS);
    };
    resetIdle();
```

在 stdout 与 stderr 的 `on("data")` 回调**开头**都调 `resetIdle()`;在 `close` 与 `error` 回调**开头**都 `if (idleTimer) clearTimeout(idleTimer);`。

④ stderr 尾环形缓冲:`new Promise` 内加 `let stderrTail = "";`;stderr data 回调里(现 L118-121)累积 `stderrTail = (stderrTail + msg).slice(-2048);`(保留现有 `console.warn`);异常退出判定处(现 L126-128)把尾附上:

```ts
      if (code !== 0 && !signal?.aborted && !producedOutput && !errorEmitted) {
        const tail = stderrTail.trim() ? `\n${stderrTail.trim()}` : "";
        handle({ type: "error", message: `${adapter.id} 进程异常退出 (code=${code})${tail}` });
      }
```

⑤ **两处** `resolve(fullText)` 改为 `resolve({ fullText, cliSessionId })`:close 回调(现 L130)、error 回调(现 L141);`new Promise<string>`(L110)改 `new Promise<{ fullText: string; cliSessionId?: string }>`。

- [ ] **Step 5: 跑测试确认通过 + 回归**

Run: `pnpm --filter @pocket-code/server test -- runner`
Expected: 新例 PASS;既有 runner 测试若断言了旧 string 返回值,同步改为 `.fullText`(cli/index.ts 是唯一生产调用方,T7 处理)。

- [ ] **Step 6: Commit**

```bash
git add packages/server/src/cli/runner.ts packages/server/src/cli/runner.test.ts packages/server/src/cli/types.ts
git commit -m "feat(server): CLI runner 返回对象化(cliSessionId)+ 空闲超时 120s + stderr 尾附错误 + resume 契约"
```

---

### Task 7: claude --resume 全链 + codex/gemini 历史注入

**Files:**
- Modify: `packages/server/src/cli/claudeCode.ts`(extractSessionId + buildSpawn 加 --resume)
- Modify: `packages/server/src/cli/claudeCode.test.ts`(补断言)
- Modify: `packages/server/src/cli/index.ts`(`runCliSession` 织入 resume 读写 + 历史注入 + 消费对象返回值)
- Modify: `packages/server/src/agent.ts`(`AgentSession` 加 `cliSessions?`;现 L98 起接口)

**Interfaces:**
- Consumes: T6 `runCliAgent` 返回 `{ fullText, cliSessionId? }`、`CliSpawnContext.resumeSessionId`、`CliAgentAdapter.extractSessionId`。
- Produces: 完整跨轮记忆;无下游。

**背景:** claude stream-json 首条 init 消息形如 `{"type":"system","subtype":"init","session_id":"...","...":...}`。捕获后存 `AgentSession.cliSessions["claude-code"]`,下轮 buildSpawn 前插 `--resume <id>`。codex/gemini(supportsResume:false)无此机制,由 runCliSession 在有历史时把近 6 轮摘要注入 userMessage。

- [ ] **Step 1: AgentSession 加字段**

`packages/server/src/agent.ts` 的 `AgentSession` 接口(现 L98 起)加:

```ts
  /** CLI 委托路径的续接会话 id,按 adapter.id 分槽(claude 的 --resume)。 */
  cliSessions?: Record<string, string>;
```

- [ ] **Step 2: claude adapter 测试**

在 `packages/server/src/cli/claudeCode.test.ts` 加:

```ts
describe("claudeCode extractSessionId", () => {
  it("从 init 消息取 session_id", () => {
    const sid = claudeCodeAdapter.extractSessionId!('{"type":"system","subtype":"init","session_id":"sess_abc"}');
    expect(sid).toBe("sess_abc");
  });
  it("非 init 消息返回 undefined", () => {
    expect(claudeCodeAdapter.extractSessionId!('{"type":"assistant"}')).toBeUndefined();
  });
  it("坏 JSON 安全返回 undefined", () => {
    expect(claudeCodeAdapter.extractSessionId!("not json")).toBeUndefined();
  });
});

describe("claudeCode buildSpawn --resume", () => {
  it("有 resumeSessionId 时 args 含 --resume <id>", () => {
    const spec = claudeCodeAdapter.buildSpawn("hi", { workspace: "/ws", resumeSessionId: "sess_x" });
    const i = spec.args.indexOf("--resume");
    expect(i).toBeGreaterThan(-1);
    expect(spec.args[i + 1]).toBe("sess_x");
  });
  it("无 resumeSessionId 时不含 --resume", () => {
    const spec = claudeCodeAdapter.buildSpawn("hi", { workspace: "/ws" });
    expect(spec.args).not.toContain("--resume");
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/server test -- claudeCode`
Expected: FAIL — extractSessionId 不存在 / 无 --resume。

- [ ] **Step 4: claude adapter 实现**

`claudeCode.ts` 的 `claudeCodeAdapter` 对象加 `extractSessionId`:

```ts
  extractSessionId(line: string): string | undefined {
    const t = line.trim();
    if (!t) return undefined;
    try {
      const m = JSON.parse(t);
      if (m?.type === "system" && m?.subtype === "init" && typeof m.session_id === "string") {
        return m.session_id;
      }
    } catch { /* 非 JSON 行 */ }
    return undefined;
  },
```

`buildSpawn` 里(args 数组构造后、return 前)插 resume:

```ts
    if (ctx.resumeSessionId) {
      args.unshift("--resume", ctx.resumeSessionId);
    }
```

(unshift 放最前;`-p` 等参数顺序不受影响 —— claude CLI 接受 flag 任意序。)

- [ ] **Step 5: 跑 claude 测试确认通过**

Run: `pnpm --filter @pocket-code/server test -- claudeCode`
Expected: 5 例 PASS。

- [ ] **Step 6: runCliSession 织入(index.ts)**

`packages/server/src/cli/index.ts` 的 `runCliSession`(现 L23-41)改为:

```ts
const HISTORY_TURNS = 6;
const HISTORY_CHAR_CAP = 500;

/** codex/gemini 无 resume:把近 N 轮历史摘要注入 userMessage 前缀。
 *  session.messages 是 `ai` SDK 的 CoreMessage[](content 为 string | Array),
 *  故用 typeof 分流;所有 role variant 都有 content 字段,tool-role 也安全。 */
function injectHistory(session: AgentSession, userMessage: string): string {
  const recent = session.messages.slice(-HISTORY_TURNS * 2);
  if (recent.length === 0) return userMessage;
  const lines = recent.map((m) => {
    const text = typeof m.content === "string" ? m.content : JSON.stringify(m.content);
    return `${m.role}: ${text.slice(0, HISTORY_CHAR_CAP)}`;
  });
  return `## Recent conversation\n${lines.join("\n")}\n\n## Current request\n${userMessage}`;
}

export async function runCliSession(
  adapter: CliAgentAdapter,
  session: AgentSession,
  userMessage: string,
  onEvent: (ev: AgentEventType) => void,
  signal?: AbortSignal,
  spawnFn?: SpawnFn
): Promise<void> {
  const effectiveMessage = adapter.supportsResume ? userMessage : injectHistory(session, userMessage);
  const resumeSessionId = adapter.supportsResume ? session.cliSessions?.[adapter.id] : undefined;
  console.log(`[CLI] ${adapter.id}: workspace=${session.workspace}, resume=${resumeSessionId ?? "none"}, msg="${userMessage.slice(0, 80)}"`);
  const { fullText, cliSessionId } = await runCliAgent(
    adapter,
    effectiveMessage,
    { workspace: session.workspace, customPrompt: session.customPrompt, resumeSessionId },
    onEvent,
    signal,
    spawnFn
  );
  if (cliSessionId) {
    session.cliSessions = { ...(session.cliSessions ?? {}), [adapter.id]: cliSessionId };
  }
  session.messages.push({ role: "assistant", content: fullText || `(${adapter.id} completed)` });
}
```

**注意** `injectHistory` 用的是 push 之前的 `session.messages` —— 但 agent.ts:162 在调 runCliSession **之前**已 push 了本轮 user 消息。故 `slice(-12)` 会含本轮 user(它就是 userMessage)。修正:injectHistory 里排除最后一条(本轮 user):`const recent = session.messages.slice(0, -1).slice(-HISTORY_TURNS * 2);`。

- [ ] **Step 7: 补 runCliSession 测试(index.test.ts;无则新建)**

```ts
import { describe, expect, it, vi } from "vitest";

describe("runCliSession 历史注入与 resume", () => {
  it("supportsResume:false 且有历史 → userMessage 前缀含 Recent conversation", async () => {
    const captured: { msg?: string; ctx?: any } = {};
    vi.doMock("./runner.js", () => ({
      runCliAgent: vi.fn(async (_a: any, msg: string, ctx: any) => { captured.msg = msg; captured.ctx = ctx; return { fullText: "ok" }; }),
    }));
    const { runCliSession } = await import("./index.js");
    const session: any = { workspace: "/ws", messages: [
      { role: "user", content: "prev q" }, { role: "assistant", content: "prev a" },
      { role: "user", content: "new q" }, // 本轮已被 agent.ts push
    ] };
    await runCliSession({ id: "codex", supportsResume: false } as any, session, "new q", () => {});
    expect(captured.msg).toContain("## Recent conversation");
    expect(captured.msg).toContain("prev q");
    expect(captured.ctx.resumeSessionId).toBeUndefined();
    vi.doUnmock("./runner.js");
  });

  it("supportsResume:true → 不注历史,传 resumeSessionId,回写 cliSessions", async () => {
    vi.doMock("./runner.js", () => ({
      runCliAgent: vi.fn(async () => ({ fullText: "ok", cliSessionId: "sess_new" })),
    }));
    const { runCliSession } = await import("./index.js");
    const session: any = { workspace: "/ws", messages: [{ role: "user", content: "q" }], cliSessions: { "claude-code": "sess_old" } };
    let passedCtx: any;
    // 用 spy 抓 ctx:改用上例 captured 模式亦可
    await runCliSession({ id: "claude-code", supportsResume: true } as any, session, "q", () => {});
    expect(session.cliSessions["claude-code"]).toBe("sess_new");
    vi.doUnmock("./runner.js");
  });
});
```

(vi.doMock + 动态 import 是 vitest 隔离 ESM 依赖的惯用法;若 runner.test 已有更简单的注入路径,对齐之。)

- [ ] **Step 8: 全验证 + Commit**

```bash
cd packages/server && npx tsc --noEmit && cd ../..
pnpm --filter @pocket-code/server test
git add packages/server/src/cli/claudeCode.ts packages/server/src/cli/claudeCode.test.ts packages/server/src/cli/index.ts packages/server/src/cli/index.test.ts packages/server/src/agent.ts
git commit -m "feat(server): claude --resume 全链(session_id 捕获→cliSessions→下轮 resume)+ codex/gemini 近6轮历史注入"
```
Expected: tsc 0 错误、测试全绿。

---

### Task 8: 文档 —— 已知限制 + 拆分路线状态

**Files:**
- Modify: `packages/server/README.md`(若无相关节则加"执行层能力"节;以文件实际结构为准)
- Modify: `plan.md`(拆分路线 client-core 行更新 + cli 层缺陷已修记录)

**Interfaces:** 无。

**背景:** 记录 #3/#6 为已知限制;更新 plan.md 拆分路线(client-core 已达标、cli 层缺陷已修、拆库为下一轮)。

- [ ] **Step 1: server README 加执行层能力说明**

在 `packages/server/README.md` 合适位置(以实际结构为准)加:

```markdown
## 执行层能力(后台进程 / CLI 委托)

- **后台进程**:`runInBackground`/`stopProcess` 经 `processRegistry`(daemon 级)管理,按 workspace 分组;
  session TTL 与断连**不**终止后台进程 —— dev server 活到显式 `stopProcess` 或 daemon 退出。
  同一 workspace 重复起同一命令会先杀旧再起新(防堆积)。
- **runCommand 超时**:默认 120s,模型可传 `timeoutSeconds`(1..600)。
- **CLI 委托记忆**:claude-code 用底层 `--resume`(捕获 stream-json 的 session_id;server 重启/会话过期则降级为新会话);
  codex/gemini 无原生续接,由 server 注入近 6 轮对话摘要。
- **已知限制**:① CLI(如 claude)自身在其内部 shell 里起的后台服务,生命周期归该 CLI 管,
  pocket-code 不追踪(请让 CLI 用自身后台机制);② docker 模式下 `stopProcess` 用容器内 `pkill -f <command>` 匹配终止。
```

- [ ] **Step 2: plan.md 更新**

`plan.md` 拆分路线节(`## 拆分路线`)的 client-core 行:

```markdown
- **client-core + Web 端** → P10 候选。
```

改为:

```markdown
- **client-core + Web 端** → ✅ 已达"随时可迁"(P10 建包 + P11 删冻结副本后为 App/Web 唯一真相源)。
```

在拆分路线节末尾加一行:

```markdown
- **cli 适配层** 的执行层功能债已于 2026-07-11 修复(后台进程/resume/超时/prompt 门控,spec/plan 见 docs/superpowers/{specs,plans}/2026-07-11-cli执行层缺陷修复*);拆库(git subtree + npm 发布)为紧接的下一轮。
```

- [ ] **Step 3: 最终全量验证 + Commit**

```bash
pnpm test:all
git add packages/server/README.md plan.md
git commit -m "docs: 执行层能力与已知限制说明 + 拆分路线状态更新(client-core 达标/cli 层缺陷已修)"
```
Expected: test:all EXIT 0。

---

## Self-Review 记录(plan 定稿前自查)

1. **Spec 覆盖**:§3A(#1 #8)→ T1(提取)+T2(注册表)+T3(接线+入口清理);§3B(#7)→T4;§3C(#9)→T5;§3E(#4 runner 空闲超时)→T6;§3F(#5 stderr)→T6;§3D(#2 记忆)→T6(采集/契约)+T7(claude resume+历史注入);§8 文档→T8。范围外(#3/#6)→ T8 README 记录。无缺口。
2. **占位符扫描**:无 TBD;所有代码步骤含完整代码;命令含预期输出。T5 Step 1、T7 index.test 的 vi.doMock 标注了"以实际为准"的核对动作(prompt 现签名、runner.test 既有 helper)——这是必要的现场核对而非占位(计划已给出两种签名分支的处理)。
3. **类型一致性**:`runCliAgent` 返回 `{ fullText, cliSessionId? }` 在 T6 定义、T7 消费一致;`CliSpawnContext.resumeSessionId?`/`CliAgentAdapter.extractSessionId?` T6 定义、T7(claude)实现/(index)消费一致;`AgentSession.cliSessions?: Record<string,string>` T7 定义与 index.ts 读写一致;`startManaged(workspace, command, {containerId?})` T2 定义与 T3 nodeBackend 调用一致;`killProcessTree` T1 定义与 T2/T6 使用一致;`buildSystemPrompt({customPrompt?, supportsBackground?})` T5 定义与 agent.ts 调用一致。
4. **注入历史的 off-by-one**:agent.ts:162 先 push 本轮 user 再调 runCliSession,故 injectHistory 必须 `slice(0,-1)` 排除本轮 user(已在 T7 Step 6 注明修正)。
