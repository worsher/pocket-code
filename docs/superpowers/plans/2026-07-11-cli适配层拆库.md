# CLI 适配层拆库(@pocket-code/cli-agent)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `packages/server/src/cli/` 的 CLI 适配层抽成 monorepo 内独立包 `@pocket-code/cli-agent`(零运行时依赖、自有 CliEvent、发布就绪),server 仅留薄包装,达到"随时可 git subtree 抽出"终态。

**Architecture:** 库自定义 7 变体 `CliEvent`(纯 TS,字段与 wire 逐字对齐)+ 运行时守卫 `isCliEvent`(替代测试里的 wire zod 校验);server 侧恒等映射 `toAgentEvent`(编译期漂移哨兵);runCliSession/injectHistory/session 读写留 server。**迁移=复制策略**:T2–T5 把文件复制进库(server 原件继续运行),T7 一次性 rewire + 删除 server 原件 —— 每个任务结束时全仓测试都绿。Spec:`docs/superpowers/specs/2026-07-11-cli适配层拆库-design.md`。

**Tech Stack:** TypeScript(ESM,`.js` 导入后缀)、vitest(**不是 jest**)、pnpm workspace。

## Global Constraints

- 分支:`feature/cli-agent-split`(从 master 切出)。
- **库零运行时依赖**:`packages/cli-agent/package.json` 的 `dependencies` 恒为 `{}`;devDependencies 里**也不得有** `@pocket-code/wire`(git subtree 抽出后外部无法安装私有包)。
- **逐字迁移纪律**:被迁文件除本计划枚举的 swap 行(import 行、`AgentEventType`→`CliEvent`、`AgentEvent.safeParse(x).success`→`isCliEvent(x)`、claudeCode.e2e 的陈旧返回形状修正)外,**一字不改**。每个迁移步骤附 `diff` 验证命令,预期输出只含枚举行。
- **迁移=复制**:T2–T5 用 `cp` 复制进库,server 原件**保留**(继续被 server 测试驱动);全部删除集中在 T7(rewire 同 commit)。任何任务结束时 `pnpm --filter @pocket-code/server test` 与 `pnpm --filter @pocket-code/cli-agent test` 都必须绿。
- **发布件形态照 `packages/protocol-core/package.json` 实况**(name/version/description/private/license MIT/repository+directory/keywords/type:module/main:dist/types:dist/scripts{build,test})—— spec 提到的"exports/files 齐"以此形态落实(main/types 即入口声明)。
- **build 顺序**:server 经 `main: dist/index.js` 解析库 → 每次改动库源码后、跑 server 侧验证前,必须 `pnpm --filter @pocket-code/cli-agent build`(与 agent-core 既有惯例相同)。
- ESM:相对 import 带 `.js` 后缀;node 内置带 `node:` 前缀。
- macOS 无 `timeout` 命令,不要用它包裹。
- 验证门:各任务末尾相关包测试全绿;末任务 `pnpm test:all` EXIT 0(**注意 test:all 是显式 filter 清单**,T1 必须把新包加进去)。
- 提交信息中文,`feat:`/`refactor:`/`docs:` 前缀,结尾逐字带:

```
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01VTWQvYMuNm4MxVzL3QQyK5
```

## File Structure(全景)

| 文件 | 动作 | 任务 |
|---|---|---|
| `packages/cli-agent/{package.json,tsconfig.json}` | 建(包骨架) | T1 |
| `packages/cli-agent/src/types.ts` | 建(CliEvent + 三接口) | T1 |
| `packages/cli-agent/src/isCliEvent.ts` + `.test.ts` | 建(运行时守卫,TDD) | T1 |
| 根 `package.json` 的 `test:all` | 改(加 `--filter @pocket-code/cli-agent`) | T1 |
| `packages/cli-agent/src/processKill.ts` + `.test.ts` | 复制自 server/src | T2 |
| `packages/server/src/processRegistry.ts:5` | 改(import 换库) | T2 |
| `packages/cli-agent/src/runner.ts` + `.test.ts` | 复制 + swap | T3 |
| `packages/cli-agent/src/{claudeCode,codex,gemini}.ts` + 各 `.test.ts` | 复制 + swap | T4 |
| `packages/cli-agent/src/{claudeCode,codex,gemini}.e2e.test.ts` | 复制 + swap + 修陈旧断言 | T5 |
| `packages/cli-agent/src/index.ts` + `index.test.ts`(smoke+隔离断言) | 建 | T6 |
| `packages/server/src/cli/index.ts` | 重写薄包装 | T7 |
| `packages/server/src/cli/index.test.ts` | 改(mock 换指向库) | T7 |
| server 侧 12 个已迁原件 | 删除 | T7 |
| `packages/cli-agent/README.md` + `plan.md` | 建/改 | T8 |

---

### Task 1: 包骨架 + CliEvent 类型 + isCliEvent 守卫

**Files:**
- Create: `packages/cli-agent/package.json`
- Create: `packages/cli-agent/tsconfig.json`
- Create: `packages/cli-agent/src/types.ts`
- Create: `packages/cli-agent/src/isCliEvent.ts`
- Create: `packages/cli-agent/src/isCliEvent.test.ts`
- Modify: 根 `package.json`(`test:all` 脚本)

**Interfaces:**
- Produces(后续全部任务依赖,签名逐字):
  - `CliEvent`(7 变体判别联合,见下)
  - `CliAgentAdapter` / `CliSpawnContext` / `CliSpawnSpec`(与 server/src/cli/types.ts 现有形状一致,仅 `AgentEventType`→`CliEvent`)
  - `isCliEvent(v: unknown): v is CliEvent`

**背景(零上下文实现者):** monorepo 用 pnpm workspace(`pnpm-workspace.yaml` 的 packages glob 覆盖 `packages/*`,新目录自动入 workspace —— 先 `cat pnpm-workspace.yaml` 确认)。新包是纯 TS 库,零运行时依赖;测试里原来用 `@pocket-code/wire` 的 zod `AgentEvent.safeParse` 校验事件,库不能依赖 wire,故自带手写守卫 `isCliEvent`(这也是给库消费者的边界校验工具)。

- [ ] **Step 1: 建包骨架**

`packages/cli-agent/package.json`(形态照 protocol-core):

```json
{
  "name": "@pocket-code/cli-agent",
  "version": "0.1.0",
  "description": "Drive coding-agent CLIs (claude-code / codex / gemini-cli) as child processes and normalize their NDJSON output into a unified CliEvent stream",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/worsher/pocket-code.git",
    "directory": "packages/cli-agent"
  },
  "keywords": [
    "claude-code",
    "codex",
    "gemini-cli",
    "cli-agent",
    "ndjson"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "test": "vitest run src" },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
```

`packages/cli-agent/tsconfig.json`(逐字照 agent-core):

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "declaration": true,
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src"]
}
```

然后 `pnpm install`(让 workspace 链接新包)。

- [ ] **Step 2: 写 `types.ts`**

```ts
// ── CLI Agent 契约 ─────────────────────────────────────────
// 本库的归一化事件类型 CliEvent:字段名与 pocket-code wire 协议的对应
// 变体逐字相同(消费方可用编译期恒等函数映射,漂移即 tsc 报错)。
// 纯 TS 类型,零运行时依赖;运行时校验用 isCliEvent(./isCliEvent.js)。

/** CLI 适配层产出的归一化事件(7 变体判别联合)。 */
export type CliEvent =
  | { type: "text-delta"; text: string }
  | { type: "reasoning-delta"; text: string }
  | { type: "tool-call"; callId: string; name: string; args: Record<string, unknown> }
  | { type: "tool-result"; callId: string; result: unknown; isError?: boolean }
  | { type: "usage"; inputTokens: number; outputTokens: number }
  | { type: "done" }
  | { type: "error"; message: string; code?: string };

/** 一次用户轮次的 spawn 上下文。 */
export interface CliSpawnContext {
  /** 代理执行的工作目录(workspace 绝对路径)。 */
  workspace: string;
  /** 可选的项目级系统指令(注入 CLI)。 */
  customPrompt?: string;
  /** claude 续接:上轮捕获的 session_id(有则 buildSpawn 加 --resume)。 */
  resumeSessionId?: string;
}

/** 子进程 spawn 规格(由上层运行器据此 spawn)。 */
export interface CliSpawnSpec {
  cmd: string;
  args: string[];
  env: NodeJS.ProcessEnv;
  cwd: string;
}

export interface CliAgentAdapter {
  /** 稳定标识。 */
  readonly id: "claude-code" | "codex" | "gemini-cli";
  /** 底层 CLI 是否支持续接上一次会话。 */
  readonly supportsResume: boolean;
  /** 据用户消息与上下文构造 spawn 规格。 */
  buildSpawn(userMessage: string, ctx: CliSpawnContext): CliSpawnSpec;
  /**
   * 解析 CLI stdout 的一行 NDJSON,返回归一化 CliEvent 数组。
   * 对空行/非 JSON 行/无对应业务语义的类型,返回 []。
   * 无状态适配器实现本方法;有状态的实现 createParser。二者至少其一。
   */
  parseLine?(line: string): CliEvent[];
  /**
   * 可选:创建一次运行专用的解析器(每次 spawn 新建,状态互不串扰)。
   * runner 优先使用本方法。
   */
  createParser?(): (line: string) => CliEvent[];
  /** 可选:从 stdout 一行提取底层 CLI 的 session_id(claude stream-json init 消息)。首次命中即被 runner 记住。 */
  extractSessionId?(line: string): string | undefined;
}
```

(注释与 server/src/cli/types.ts 现有文案逐字保留,仅 AgentEventType→CliEvent 与头注释更新。写完后 `diff <(git show HEAD:packages/server/src/cli/types.ts) packages/cli-agent/src/types.ts` 核对差异仅在:头注释块、wire import 行删除、CliEvent 定义新增、接口内 AgentEventType→CliEvent。)

- [ ] **Step 3: 写守卫测试 `isCliEvent.test.ts`(先失败)**

```ts
import { describe, expect, it } from "vitest";
import { isCliEvent } from "./isCliEvent.js";

describe("isCliEvent", () => {
  it("接受全部 7 个变体", () => {
    expect(isCliEvent({ type: "text-delta", text: "hi" })).toBe(true);
    expect(isCliEvent({ type: "reasoning-delta", text: "t" })).toBe(true);
    expect(isCliEvent({ type: "tool-call", callId: "c1", name: "readFile", args: { path: "a" } })).toBe(true);
    expect(isCliEvent({ type: "tool-result", callId: "c1", result: { ok: 1 } })).toBe(true);
    expect(isCliEvent({ type: "tool-result", callId: "c1", result: "s", isError: true })).toBe(true);
    expect(isCliEvent({ type: "usage", inputTokens: 10, outputTokens: 20 })).toBe(true);
    expect(isCliEvent({ type: "done" })).toBe(true);
    expect(isCliEvent({ type: "error", message: "boom" })).toBe(true);
    expect(isCliEvent({ type: "error", message: "boom", code: "E1" })).toBe(true);
  });

  it("拒绝未知 type 与非对象", () => {
    expect(isCliEvent({ type: "totally-unknown" })).toBe(false);
    expect(isCliEvent(null)).toBe(false);
    expect(isCliEvent("text-delta")).toBe(false);
    expect(isCliEvent(undefined)).toBe(false);
  });

  it("拒绝缺字段/错类型", () => {
    expect(isCliEvent({ type: "text-delta" })).toBe(false); // 缺 text
    expect(isCliEvent({ type: "tool-call", callId: "c1", args: {} })).toBe(false); // 缺 name
    expect(isCliEvent({ type: "tool-call", callId: "c1", name: "x", args: "not-obj" })).toBe(false);
    expect(isCliEvent({ type: "usage", inputTokens: -1, outputTokens: 0 })).toBe(false); // 负数
    expect(isCliEvent({ type: "usage", inputTokens: 1.5, outputTokens: 0 })).toBe(false); // 非整数
    expect(isCliEvent({ type: "error" })).toBe(false); // 缺 message
    expect(isCliEvent({ type: "tool-result", callId: "c1", result: 1, isError: "yes" })).toBe(false); // isError 非 bool
  });
});
```

- [ ] **Step 4: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/cli-agent test`
Expected: FAIL — `Cannot find module './isCliEvent.js'`。

- [ ] **Step 5: 实现 `isCliEvent.ts`**

```ts
// CliEvent 的运行时守卫:库消费者在边界(如 IPC/持久化)校验事件用;
// 本库测试也用它替代原先对 pocket-code wire zod schema 的依赖。
import type { CliEvent } from "./types.js";

function isObj(v: unknown): v is Record<string, unknown> {
  return typeof v === "object" && v !== null;
}

function isNonNegInt(v: unknown): boolean {
  return typeof v === "number" && Number.isInteger(v) && v >= 0;
}

export function isCliEvent(v: unknown): v is CliEvent {
  if (!isObj(v) || typeof v.type !== "string") return false;
  switch (v.type) {
    case "text-delta":
    case "reasoning-delta":
      return typeof v.text === "string";
    case "tool-call":
      return typeof v.callId === "string" && typeof v.name === "string" && isObj(v.args);
    case "tool-result":
      return typeof v.callId === "string" && (v.isError === undefined || typeof v.isError === "boolean");
    case "usage":
      return isNonNegInt(v.inputTokens) && isNonNegInt(v.outputTokens);
    case "done":
      return true;
    case "error":
      return typeof v.message === "string" && (v.code === undefined || typeof v.code === "string");
    default:
      return false;
  }
}
```

- [ ] **Step 6: 跑测试确认通过 + tsc**

Run: `pnpm --filter @pocket-code/cli-agent test && cd packages/cli-agent && npx tsc --noEmit && cd ../..`
Expected: 3 测试块全 PASS;tsc 0 错误。

- [ ] **Step 7: test:all 加入新包**

根 `package.json` 的 `test:all` 行,在 `--filter @pocket-code/client-core` 之后插入 `--filter @pocket-code/cli-agent`:

旧(一行,以文件实况为准):
```
"test:all": "pnpm --filter @pocket-code/wire --filter @pocket-code/protocol-core --filter @pocket-code/agent-core --filter @pocket-code/client-core --filter @pocket-code/server ..."
```
新:
```
"test:all": "pnpm --filter @pocket-code/wire --filter @pocket-code/protocol-core --filter @pocket-code/agent-core --filter @pocket-code/client-core --filter @pocket-code/cli-agent --filter @pocket-code/server ..."
```

- [ ] **Step 8: Commit**

```bash
git add packages/cli-agent package.json pnpm-lock.yaml
git commit -m "feat(cli-agent): 包骨架 + CliEvent 类型 + isCliEvent 运行时守卫(拆库 T1)"
```

---

### Task 2: processKill 复制进库 + processRegistry 改指库

**Files:**
- Create: `packages/cli-agent/src/processKill.ts`(复制自 `packages/server/src/processKill.ts`)
- Create: `packages/cli-agent/src/processKill.test.ts`(复制自 server 侧同名)
- Modify: `packages/server/src/processRegistry.ts:5`(import 换)
- Modify: `packages/server/package.json`(加依赖)

**Interfaces:**
- Produces:库内 `killProcessTree(pid, graceMs?)` / `isProcessAlive(pid)`(T3 runner、server processRegistry 消费)。

**背景:** killProcessTree 是跨平台进程树终止(SIGTERM→grace→SIGKILL,进程组信号;win32 taskkill)。按 spec D3 进库为单一真相源。**server 原件本任务不删**(server/src/cli/runner.ts 还在 import 它,T7 才删)——只把 processRegistry 的 import 切到库。

- [ ] **Step 1: 复制两文件**

```bash
cp packages/server/src/processKill.ts packages/cli-agent/src/processKill.ts
cp packages/server/src/processKill.test.ts packages/cli-agent/src/processKill.test.ts
```

复制后逐字核对(唯一预期差异:**无**——两文件内容与 server 版完全一致,import 都是包内相对路径与 node 内置):

```bash
diff packages/server/src/processKill.ts packages/cli-agent/src/processKill.ts && echo IDENTICAL
diff packages/server/src/processKill.test.ts packages/cli-agent/src/processKill.test.ts && echo IDENTICAL
```
Expected: 两个 IDENTICAL。

- [ ] **Step 2: 库测试通过 + build**

Run: `pnpm --filter @pocket-code/cli-agent test && pnpm --filter @pocket-code/cli-agent build`
Expected: processKill 3 例 + isCliEvent 全 PASS;build 产出 dist/。

- [ ] **Step 3: server 加依赖 + processRegistry 换 import**

`packages/server/package.json` 的 `dependencies` 加(与其他 workspace 依赖并列):

```json
    "@pocket-code/cli-agent": "workspace:*",
```

然后 `pnpm install`。

`packages/server/src/processRegistry.ts:5`:

```ts
// 旧
import { killProcessTree } from "./processKill.js";
// 新
import { killProcessTree } from "@pocket-code/cli-agent";
```

**注意**:此时库的 index.ts 还不存在(T6 才建)!`main: dist/index.js` 解析会失败。**本任务先建一个最小 `packages/cli-agent/src/index.ts`**(T6 再扩成完整入口):

```ts
export { killProcessTree, isProcessAlive } from "./processKill.js";
export { isCliEvent } from "./isCliEvent.js";
export type { CliEvent, CliAgentAdapter, CliSpawnContext, CliSpawnSpec } from "./types.js";
```

再 `pnpm --filter @pocket-code/cli-agent build`(dist/index.js 就位)。

- [ ] **Step 4: server 全验证**

Run: `pnpm --filter @pocket-code/server test && cd packages/server && npx tsc --noEmit && cd ../..`
Expected: server 136 通过 3 skipped(processRegistry 测试驱动的是换过 import 的代码);tsc 0 错误。

- [ ] **Step 5: Commit**

```bash
git add packages/cli-agent packages/server/package.json packages/server/src/processRegistry.ts pnpm-lock.yaml
git commit -m "feat(cli-agent): processKill 迁入库 + server processRegistry 改从库导入(拆库 T2,server 原件留至 T7 删)"
```

---

### Task 3: runner 复制进库(swap)

**Files:**
- Create: `packages/cli-agent/src/runner.ts`(复制 `packages/server/src/cli/runner.ts` + swap)
- Create: `packages/cli-agent/src/runner.test.ts`(复制同名 + swap)

**Interfaces:**
- Consumes: T1 `CliEvent`/`CliAgentAdapter`/`CliSpawnContext`、T2 `killProcessTree`。
- Produces: `runCliAgent(adapter, userMessage, ctx, onEvent: (e: CliEvent) => void, signal?, spawnFn?): Promise<{ fullText: string; cliSessionId?: string }>`、`export type SpawnFn`(T5 e2e、T6 入口、T7 server 薄包装消费)。

**背景:** runner 驱动子进程、行缓冲、abort、120s 空闲超时、stderr 尾、session_id 采集。**逻辑一字不改**,只 swap 类型来源。

- [ ] **Step 1: 复制 + swap `runner.ts`**

```bash
cp packages/server/src/cli/runner.ts packages/cli-agent/src/runner.ts
```

swap(仅这三处):
1. 删 `import type { AgentEventType } from "@pocket-code/wire";`
2. `import type { CliAgentAdapter, CliSpawnContext } from "./types.js";` 行**扩为** `import type { CliAgentAdapter, CliSpawnContext, CliEvent } from "./types.js";`
3. `import { killProcessTree } from "../processKill.js";` → `import { killProcessTree } from "./processKill.js";`
4. 全文 `AgentEventType` → `CliEvent`(出现在 onEvent 参数类型与 handle/drainLine 的局部类型标注)。

验证只有枚举行变化:

```bash
diff <(git show HEAD:packages/server/src/cli/runner.ts) packages/cli-agent/src/runner.ts
```
Expected: 差异仅上述 import 行与 `AgentEventType`→`CliEvent` 的替换行。

- [ ] **Step 2: 复制 + swap `runner.test.ts`**

```bash
cp packages/server/src/cli/runner.test.ts packages/cli-agent/src/runner.test.ts
```

swap:
1. `import type { AgentEventType } from "@pocket-code/wire";` → `import type { CliEvent } from "./types.js";`
2. 全文 `AgentEventType` → `CliEvent`(15 处 `const events: AgentEventType[]` 等)。
3. 其余 import(`./runner.js`/`./claudeCode.js`)不变 —— **注意** runner.test 引用了 `claudeCodeAdapter`(claudeCode.ts T4 才迁)!确认:`grep -n "claudeCode" packages/cli-agent/src/runner.test.ts`。若引用存在,本任务测试会因模块缺失而挂 —— **处理**:本任务先把 `packages/server/src/cli/claudeCode.ts` 也复制+swap 进库(swap 规则见 T4 Step 1,完全相同),但 claudeCode 的**单测**留给 T4。在报告里说明此前置。

- [ ] **Step 3: 库测试 + build**

Run: `pnpm --filter @pocket-code/cli-agent test && pnpm --filter @pocket-code/cli-agent build`
Expected: runner 13 例(含 idle 超时/stderr 尾/session_id 采集)全 PASS;tsc(build)0 错误。

- [ ] **Step 4: server 不回归**

Run: `pnpm --filter @pocket-code/server test`
Expected: 136 通过(server 原件仍在,未受影响)。

- [ ] **Step 5: Commit**

```bash
git add packages/cli-agent/src
git commit -m "feat(cli-agent): runner 迁入库(AgentEventType→CliEvent swap,逻辑零变化;连带 claudeCode 源以满足 runner.test 引用)(拆库 T3)"
```

---

### Task 4: 三适配器 + 单测复制进库(swap)

**Files:**
- Create: `packages/cli-agent/src/claudeCode.ts`(若 T3 已复制则跳过源文件)+ `claudeCode.test.ts`
- Create: `packages/cli-agent/src/codex.ts` + `codex.test.ts`
- Create: `packages/cli-agent/src/gemini.ts` + `gemini.test.ts`

**Interfaces:**
- Consumes: T1 类型、T1 `isCliEvent`。
- Produces: `claudeCodeAdapter`/`codexAdapter`/`geminiAdapter`(均 `satisfies CliAgentAdapter`;T5 e2e、T6 入口消费)。

**背景:** 三适配器把各 CLI 的 NDJSON 归一化为事件。claude/codex 用 `parseLine`(无状态),gemini 用 `createParser`(有状态)。单测原来用 wire 的 zod `AgentEvent.safeParse(ev).success` 校验事件合法性 → 换 `isCliEvent(ev)`。**解析逻辑一字不改**。

- [ ] **Step 1: 复制 + swap 三个源文件**

```bash
cp packages/server/src/cli/codex.ts packages/cli-agent/src/codex.ts
cp packages/server/src/cli/gemini.ts packages/cli-agent/src/gemini.ts
# claudeCode.ts 若 T3 已复制则跳过;否则同样 cp
```

每个文件 swap(与 T3 runner 相同规则):
1. 删 `import type { AgentEventType } from "@pocket-code/wire";`(codex.ts:7 / gemini.ts:5 / claudeCode.ts:5)
2. types import 行加 `CliEvent`:`import type { CliAgentAdapter, CliSpawnContext, CliSpawnSpec, CliEvent } from "./types.js";`
3. 全文 `AgentEventType` → `CliEvent`(claudeCode 2 处、codex 2 处、gemini 2 处)。

逐个 diff 验证:

```bash
for f in claudeCode codex gemini; do
  echo "=== $f ==="
  diff <(git show HEAD:packages/server/src/cli/$f.ts) packages/cli-agent/src/$f.ts
done
```
Expected: 每个只有 import 行与 CliEvent 替换行。

- [ ] **Step 2: 复制 + swap 三个单测**

```bash
cp packages/server/src/cli/claudeCode.test.ts packages/cli-agent/src/claudeCode.test.ts
cp packages/server/src/cli/codex.test.ts packages/cli-agent/src/codex.test.ts
cp packages/server/src/cli/gemini.test.ts packages/cli-agent/src/gemini.test.ts
```

每个 swap:
1. `import { AgentEvent } from "@pocket-code/wire";`(claudeCode.test:2 / codex.test:2)→ `import { isCliEvent } from "./isCliEvent.js";`;gemini.test:2 的 `import { AgentEvent, type AgentEventType } from "@pocket-code/wire";` → `import { isCliEvent } from "./isCliEvent.js";` + `import type { CliEvent } from "./types.js";`
2. `AgentEvent.safeParse(ev).success` → `isCliEvent(ev)`(claudeCode.test:129、codex.test:100、gemini.test:57;断言从 `expect(...).toBe(true)` 形态不变)。
3. gemini.test 全文 `AgentEventType` → `CliEvent`(3 处)。

- [ ] **Step 3: 库测试 + build**

Run: `pnpm --filter @pocket-code/cli-agent test && pnpm --filter @pocket-code/cli-agent build`
Expected: 三适配器单测全部 PASS(与 server 侧同名测试同断言);build 0 错误。

- [ ] **Step 4: server 不回归 + Commit**

Run: `pnpm --filter @pocket-code/server test`
Expected: 136 通过。

```bash
git add packages/cli-agent/src
git commit -m "feat(cli-agent): 三适配器+单测迁入库(AgentEvent.safeParse→isCliEvent swap,解析逻辑零变化)(拆库 T4)"
```

---

### Task 5: 三 e2e 复制进库(swap + 修陈旧断言)

**Files:**
- Create: `packages/cli-agent/src/claudeCode.e2e.test.ts` / `codex.e2e.test.ts` / `gemini.e2e.test.ts`(复制 + swap)

**Interfaces:**
- Consumes: T3 `runCliAgent`、T4 三适配器、T1 `isCliEvent`。

**背景:** e2e 驱动**真实 CLI**(`RUN_CLI_E2E=1` 且本机装了对应 CLI 才跑,`describe.skipIf` 默认 skip —— 日常 CI 显示 3 skipped)。swap 同前;**另有一处陈旧断言必须修**:claudeCode.e2e 的 `const text = await runCliAgent(...)` 写于 runner 返回 string 的年代,T6(缺陷修复轮)对象化后它已隐性失效(默认 skip 无人发现)—— `typeof text` 现在是 "object"。codex/gemini 的 e2e 不接返回值,无此问题。

- [ ] **Step 1: 复制 + swap**

```bash
cp packages/server/src/cli/claudeCode.e2e.test.ts packages/cli-agent/src/claudeCode.e2e.test.ts
cp packages/server/src/cli/codex.e2e.test.ts packages/cli-agent/src/codex.e2e.test.ts
cp packages/server/src/cli/gemini.e2e.test.ts packages/cli-agent/src/gemini.e2e.test.ts
```

每个 swap:
1. `import type { AgentEventType } from "@pocket-code/wire";` → `import type { CliEvent } from "./types.js";`
2. `import { AgentEvent } from "@pocket-code/wire";` → `import { isCliEvent } from "./isCliEvent.js";`
3. 全文 `AgentEventType` → `CliEvent`;`AgentEvent.safeParse(e).success` → `isCliEvent(e)`。

- [ ] **Step 2: 修 claudeCode.e2e 的陈旧返回断言**

```ts
// 旧(两处相关行)
    const text = await runCliAgent(
    ...
    expect(typeof text).toBe("string");
// 新
    const { fullText } = await runCliAgent(
    ...
    expect(typeof fullText).toBe("string");
```

- [ ] **Step 3: 验证(默认 skip 形态)+ Commit**

Run: `pnpm --filter @pocket-code/cli-agent test`
Expected: 输出含 `3 skipped`(三件 e2e 被 skipIf 跳过)+ 其余全 PASS。tsc 经 `pnpm --filter @pocket-code/cli-agent build` 0 错误(skip 不代表不编译,swap 错误会在这暴露)。

```bash
git add packages/cli-agent/src
git commit -m "feat(cli-agent): 三 e2e 迁入库(swap + 修 claudeCode.e2e 对象化前的陈旧返回断言)(拆库 T5)"
```

---

### Task 6: 库入口 index.ts + 导出面 smoke + 隔离断言

**Files:**
- Modify: `packages/cli-agent/src/index.ts`(T2 的最小版扩成完整入口)
- Create: `packages/cli-agent/src/index.test.ts`(smoke + 隔离断言)

**Interfaces:**
- Produces(T7 server 薄包装消费,逐字):`cliAdapters: Record<string, CliAgentAdapter>`、`runCliAgent`、`SpawnFn`、三 adapter 具名导出、`killProcessTree`/`isProcessAlive`、`isCliEvent`、全类型。

- [ ] **Step 1: 完整入口**

`packages/cli-agent/src/index.ts` 全文替换为:

```ts
// ── @pocket-code/cli-agent 入口 ────────────────────────────
// 驱动 claude-code / codex / gemini-cli 子进程,归一化 NDJSON 输出为 CliEvent 流。
import type { CliAgentAdapter } from "./types.js";
import { claudeCodeAdapter } from "./claudeCode.js";
import { codexAdapter } from "./codex.js";
import { geminiAdapter } from "./gemini.js";

export type { CliEvent, CliAgentAdapter, CliSpawnContext, CliSpawnSpec } from "./types.js";
export { isCliEvent } from "./isCliEvent.js";
export { runCliAgent, type SpawnFn } from "./runner.js";
export { killProcessTree, isProcessAlive } from "./processKill.js";
export { claudeCodeAdapter } from "./claudeCode.js";
export { codexAdapter } from "./codex.js";
export { geminiAdapter } from "./gemini.js";

/** 按 id 索引的可用适配器。 */
export const cliAdapters: Record<string, CliAgentAdapter> = {
  [claudeCodeAdapter.id]: claudeCodeAdapter,
  [codexAdapter.id]: codexAdapter,
  [geminiAdapter.id]: geminiAdapter,
};
```

- [ ] **Step 2: smoke + 隔离断言测试 `index.test.ts`**

```ts
import { readFileSync, readdirSync } from "node:fs";
import { join, dirname } from "node:path";
import { fileURLToPath } from "node:url";
import { describe, expect, it } from "vitest";
import { cliAdapters, runCliAgent, killProcessTree, isCliEvent } from "./index.js";

const SRC = dirname(fileURLToPath(import.meta.url));

describe("入口导出面 smoke", () => {
  it("注册表含三适配器", () => {
    expect(Object.keys(cliAdapters).sort()).toEqual(["claude-code", "codex", "gemini-cli"]);
  });
  it("核心导出可用", () => {
    expect(typeof runCliAgent).toBe("function");
    expect(typeof killProcessTree).toBe("function");
    expect(typeof isCliEvent).toBe("function");
  });
});

describe("隔离断言(随时可迁不变量)", () => {
  it("src 内零 @pocket-code/wire、零 AgentSession、零越包相对引用", () => {
    for (const f of readdirSync(SRC).filter((n) => n.endsWith(".ts"))) {
      const text = readFileSync(join(SRC, f), "utf-8");
      expect(text, `${f} 引用了 wire`).not.toContain("@pocket-code/wire");
      expect(text, `${f} 引用了 AgentSession`).not.toContain("AgentSession");
      expect(text, `${f} 有越包相对引用`).not.toContain("from \"../");
    }
  });
  it("package.json dependencies 为空(零运行时依赖)", () => {
    const pkg = JSON.parse(readFileSync(join(SRC, "..", "package.json"), "utf-8"));
    expect(pkg.dependencies).toEqual({});
    expect(Object.keys(pkg.devDependencies)).not.toContain("@pocket-code/wire");
  });
});
```

- [ ] **Step 3: 验证 + Commit**

Run: `pnpm --filter @pocket-code/cli-agent test && pnpm --filter @pocket-code/cli-agent build`
Expected: smoke + 隔离断言全 PASS(隔离断言若红,说明前面某任务 swap 漏了 —— 修完再过);build 0 错误。

```bash
git add packages/cli-agent/src
git commit -m "feat(cli-agent): 完整入口导出面 + smoke + 隔离断言(零 wire/零 AgentSession/零越包/deps 空)(拆库 T6)"
```

---

### Task 7: server 薄包装重写 + 删除已迁原件

**Files:**
- Modify: `packages/server/src/cli/index.ts`(重写)
- Modify: `packages/server/src/cli/index.test.ts`(mock 换指向)
- Delete: `packages/server/src/cli/{types,runner,claudeCode,codex,gemini}.ts`
- Delete: `packages/server/src/cli/{runner,claudeCode,codex,gemini}.test.ts`
- Delete: `packages/server/src/cli/{claudeCode,codex,gemini}.e2e.test.ts`
- Delete: `packages/server/src/processKill.ts` + `packages/server/src/processKill.test.ts`

**Interfaces:**
- Consumes: T6 库入口全导出面。
- Produces: server 侧 `runCliSession`(签名不变)+ `cliAdapters` re-export —— `agent.ts:9` 的 `import { cliAdapters, runCliSession } from "./cli/index.js"` **零改动**。

**背景:** 这是 rewire+删除的原子任务。重写后的 index.ts 含:恒等映射 `toAgentEvent`(编译期漂移哨兵,spec D1 核心)、injectHistory(逐字保留)、runCliSession(逐字保留,仅 runCliAgent 来源换库 + onEvent 包映射)。`session.test.ts` 零改动(它 import 的 runCliSession/cliAdapters 都还在)。`agent.test.ts` mock 的是 `"./cli/index.js"` 路径 —— 不变,不受影响。

- [ ] **Step 1: 重写 `packages/server/src/cli/index.ts`**

全文替换为:

```ts
// ── CLI 委托的 server 薄包装 ───────────────────────────────
// 适配器/runner 本体在 @pocket-code/cli-agent(零依赖独立库);
// 本文件只做 pocket-code 特有的三件事:
// ① CliEvent → wire AgentEventType 恒等映射(字段逐字相同,编译期漂移哨兵);
// ② AgentSession 的读写(injectHistory / cliSessions 回写 / messages.push);
// ③ 向 agent.ts 保持既有导出面(cliAdapters / runCliSession)。
import type { AgentEventType } from "@pocket-code/wire";
import type { AgentSession } from "../agent.js";
import {
  cliAdapters,
  runCliAgent,
  type CliAgentAdapter,
  type CliEvent,
  type SpawnFn,
} from "@pocket-code/cli-agent";

export { cliAdapters, claudeCodeAdapter, codexAdapter, geminiAdapter } from "@pocket-code/cli-agent";
export type { CliAgentAdapter, CliSpawnContext, CliSpawnSpec } from "@pocket-code/cli-agent";

/** 恒等映射:CliEvent 各变体与 wire AgentEventType 对应变体字段逐字相同;
 *  wire 若改字段此处 tsc 立即报错(漂移哨兵),零运行时成本。 */
const toAgentEvent = (e: CliEvent): AgentEventType => e;

const HISTORY_TURNS = 6;
const HISTORY_CHAR_CAP = 500;

/** codex/gemini 无 resume:把近 N 轮历史摘要注入 userMessage 前缀。
 *  session.messages 是 `ai` SDK 的 CoreMessage[](content 为 string | Array),
 *  故用 typeof 分流;所有 role variant 都有 content 字段,tool-role 也安全。
 *  【关键 off-by-one】agent.ts 在调 runCliSession 之前已 push 本轮 user 消息,
 *  故排除最后一条(本轮 user,它就是 userMessage 本身)再取近 N 轮。 */
function injectHistory(session: AgentSession, userMessage: string): string {
  const recent = session.messages.slice(0, -1).slice(-HISTORY_TURNS * 2);
  if (recent.length === 0) return userMessage;
  const lines = recent.map((m) => {
    const text = typeof m.content === "string" ? m.content : JSON.stringify(m.content);
    // 按字符硬截断即可:注入的是给 CLI 读的历史摘要文本,非结构化数据,
    // 中途截断不破坏语义(下游 CLI 按自然语言理解),故不追求 token/句子边界。
    return `${m.role}: ${text.slice(0, HISTORY_CHAR_CAP)}`;
  });
  return `## Recent conversation\n${lines.join("\n")}\n\n## Current request\n${userMessage}`;
}

/** 通用 CLI 会话包装:运行适配器,结束后把 assistant 全文写入会话历史。
 *  claude(supportsResume:true):携带上轮捕获的 session_id 走 --resume,不注入历史摘要。
 *  codex/gemini(supportsResume:false):无原生续接,注入近 6 轮历史摘要到 userMessage。 */
export async function runCliSession(
  adapter: CliAgentAdapter,
  session: AgentSession,
  userMessage: string,
  onEvent: (ev: AgentEventType) => void,
  signal?: AbortSignal,
  spawnFn?: SpawnFn
): Promise<void> {
  const effectiveMessage = adapter.supportsResume ? userMessage : injectHistory(session, userMessage);
  const resumeSessionId = adapter.supportsResume ? session.cliSessions?.[adapter.id] : undefined;
  console.log(`[CLI] ${adapter.id}: workspace=${session.workspace}, resume=${resumeSessionId ?? "none"}, msg="${userMessage.slice(0, 80)}"`);
  const { fullText, cliSessionId } = await runCliAgent(
    adapter,
    effectiveMessage,
    { workspace: session.workspace, customPrompt: session.customPrompt, resumeSessionId },
    (e) => onEvent(toAgentEvent(e)),
    signal,
    spawnFn
  );
  if (cliSessionId) {
    session.cliSessions = { ...(session.cliSessions ?? {}), [adapter.id]: cliSessionId };
  }
  session.messages.push({ role: "assistant", content: fullText || `(${adapter.id} completed)` });
}
```

(injectHistory/runCliSession 主体与现文件逐字一致 —— 写完后核对:`git show HEAD:packages/server/src/cli/index.ts` 与新文件的差异应仅在:头注释、import 来源、re-export 来源、toAgentEvent 新增、onEvent 包装 `(e) => onEvent(toAgentEvent(e))`。)

- [ ] **Step 2: `index.test.ts` mock 换指向**

文件头部(现 L9-12)的:

```ts
const mockRunCliAgent = vi.fn();
vi.mock("./runner.js", () => ({
  runCliAgent: (...args: unknown[]) => mockRunCliAgent(...args),
}));
```

改为(**importOriginal 保留库的其余真实导出** —— index.ts 还要 re-export cliAdapters 等,整模块 mock 会砸掉它们):

```ts
const mockRunCliAgent = vi.fn();
vi.mock("@pocket-code/cli-agent", async (importOriginal) => ({
  ...(await importOriginal<typeof import("@pocket-code/cli-agent")>()),
  runCliAgent: (...args: unknown[]) => mockRunCliAgent(...args),
}));
```

头注释(L3-8 讲 doMock 竞态的)保留;其余测试体零改动。

- [ ] **Step 3: 删除已迁原件(12 文件)**

```bash
git rm packages/server/src/cli/types.ts packages/server/src/cli/runner.ts \
  packages/server/src/cli/claudeCode.ts packages/server/src/cli/codex.ts packages/server/src/cli/gemini.ts \
  packages/server/src/cli/runner.test.ts packages/server/src/cli/claudeCode.test.ts \
  packages/server/src/cli/codex.test.ts packages/server/src/cli/gemini.test.ts \
  packages/server/src/cli/claudeCode.e2e.test.ts packages/server/src/cli/codex.e2e.test.ts \
  packages/server/src/cli/gemini.e2e.test.ts \
  packages/server/src/processKill.ts packages/server/src/processKill.test.ts
```

(14 个 —— 12 个 cli 目录 + 2 个 processKill。)

- [ ] **Step 4: 全验证**

```bash
pnpm --filter @pocket-code/cli-agent build
pnpm --filter @pocket-code/server test
cd packages/server && npx tsc --noEmit && cd ../..
ls packages/server/src/cli/
```
Expected: server 测试全绿且 **skipped 归零**(3 件 e2e 已迁走)—— 预期形态 `index.test.ts` 5 例 + `session.test.ts` 4 例 + 其余 server 测试;tsc 0 错误;`ls` 恰 3 文件(index.ts / index.test.ts / session.test.ts)。

- [ ] **Step 5: agent.ts 零改动验证 + Commit**

```bash
git diff HEAD --stat -- packages/server/src/agent.ts
```
Expected: 空输出(消费端零 churn 达成)。

```bash
git add -A packages/server/src/cli
git commit -m "refactor(server): cli 目录重写为薄包装(恒等映射+session 读写)+ 删除 14 个已迁原件,agent.ts 零改动(拆库 T7)"
```

---

### Task 8: 库 README + plan.md 更新 + 全量验证

**Files:**
- Create: `packages/cli-agent/README.md`
- Modify: `plan.md`(拆分路线 cli 适配层行)

- [ ] **Step 1: 写 README**

`packages/cli-agent/README.md`:

```markdown
# @pocket-code/cli-agent

把 coding-agent CLI(claude-code / codex / gemini-cli)作为子进程驱动,把它们的 NDJSON 输出归一化为统一的 `CliEvent` 流。零运行时依赖。

## 用法

​```ts
import { cliAdapters, runCliAgent } from "@pocket-code/cli-agent";

const { fullText, cliSessionId } = await runCliAgent(
  cliAdapters["claude-code"],
  "修复 src/app.ts 里的类型错误",
  { workspace: "/abs/path/to/project", resumeSessionId: undefined },
  (e) => console.log(e)   // CliEvent 流:text-delta / tool-call / tool-result / usage / error / done
);
// claude 支持续接:把 cliSessionId 存起来,下轮放进 ctx.resumeSessionId 即 --resume。
​```

## 契约

- `CliAgentAdapter`:`buildSpawn`(构造子进程参数)+ `parseLine`/`createParser`(逐行归一化)+ 可选 `extractSessionId`(捕获底层会话 id)。
- `CliEvent`:7 变体判别联合(纯 TS 类型);运行时校验用 `isCliEvent`。
- runner 内置:120s **空闲**超时(有输出即重置)、abort 信号、进程树终止(`killProcessTree`,跨平台)、stderr 尾 2KB 附错误。

## 各适配器要点

- **claude-code**:`--output-format stream-json`;`supportsResume: true`(捕获 init 消息的 session_id → 下轮 `--resume`)。spawn 前清除宿主 shell 的 `ANTHROPIC_*` 环境变量,防止残留配置劫持 CLI(真机案例:`ANTHROPIC_MODEL` 指向已下线模型 → 404)。
- **codex / gemini-cli**:无原生续接(`supportsResume: false`),跨轮记忆由调用方注入(如把近几轮对话摘要拼进 userMessage)。
- gemini 用 `createParser`(每次运行新建解析器,状态互不串扰)。

## 测试

​```bash
pnpm --filter @pocket-code/cli-agent test        # 单测(e2e 默认 skip)
RUN_CLI_E2E=1 pnpm --filter @pocket-code/cli-agent test   # 含真实 CLI e2e(需本机装有对应 CLI)
​```
```

(README 里的代码围栏用 ``` —— 上方 ​``` 前的零宽字符仅为本计划文档转义,实写时用普通围栏。)

- [ ] **Step 2: plan.md 拆分路线更新**

`plan.md` 拆分路线节,把 cli 适配层那行(现为"独立 OSS 库(...):约 1 天,价值最高。**执行层功能债已于 2026-07-11 修复**(...),拆库(git subtree + npm 发布)为紧接的下一轮。")整行替换为:

```markdown
- **cli 适配层** → ✅ 已达"随时可迁"(2026-07-11 拆为 @pocket-code/cli-agent 独立包:零运行时依赖/自有 CliEvent+isCliEvent/隔离断言入测试;spec/plan 见 docs/superpowers/{specs,plans}/2026-07-11-cli适配层拆库*;执行层功能债同日先行修复)。真开源仅剩 git subtree 迁移 + npm 发布。
```

- [ ] **Step 3: 全量验证 + Commit**

```bash
pnpm build
pnpm test:all
```
Expected: build 全过;test:all EXIT 0,输出含 `@pocket-code/cli-agent` 的测试段(含 3 skipped e2e),server 段不再有 skipped。

```bash
git add packages/cli-agent/README.md plan.md
git commit -m "docs: cli-agent 库 README + plan.md 拆分路线更新(cli 适配层达随时可迁)(拆库 T8)"
```

---

## Self-Review 记录(plan 定稿前自查)

1. **Spec 覆盖**:§3(CliEvent+恒等映射)→T1(类型)+T7(toAgentEvent);§4 迁移布局→T2(processKill)/T3(runner)/T4(适配器)/T5(e2e)/T6(入口+发布件在 T1)/T7(server 薄包装+删除);§6 测试策略→逐字随迁(T3-T5 diff 验证)+isCliEvent 替换(T4/T5)+隔离断言(T6)+编译期恒等锚(T7 的 toAgentEvent 即 tsc 门);§7 验收→T8;§8 顺序对应 T1-T8。**修正 spec 两处实况偏差**:①"test:all 自动纳入"实为显式清单(T1 Step 7 手动加);②"发布件 exports/files"按 protocol-core 实况为 main/types 形态(Global Constraints 注明)。
2. **占位符扫描**:无 TBD;迁移任务给出 cp+枚举 swap+diff 验证(内容存在于仓库,指令完备);新写文件全部含完整代码。
3. **类型一致性**:`CliEvent` T1 定义/T3-T7 消费一致;`runCliAgent` 返回 `{fullText, cliSessionId?}` T3 产/T5 e2e 修正断言/T7 消费一致;`cliAdapters` T6 产/T7 re-export 一致;`isCliEvent` T1 产/T4/T5/T6 消费一致;T2 最小 index.ts 与 T6 完整版的导出是超集关系(T6 全文替换,无冲突)。
4. **时序坑已消**:T2 发现 `main:dist/index.js` 在 T6 前无 index → T2 内建最小入口;T3 发现 runner.test 引用 claudeCodeAdapter → T3 连带迁 claudeCode 源(单测留 T4);全部删除集中 T7(server/cli/runner.ts 在 T2-T6 间仍 import ../processKill.js,故 processKill 原件也留到 T7)。
5. **陈旧断言**:claudeCode.e2e 的 `typeof text` 是 T6(缺陷修复轮)遗留隐性失效,T5 修;codex/gemini e2e 不接返回值,确认无此问题。
