# P5 中继 HTTP 隧道预览 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让手机在 relay(公网中继)模式下也能预览开发机的 dev server 效果:relay 暴露反向 HTTP 代理 `http://relay-host/t/<machineId>/<port>/*`,把请求经 daemon WS 隧道转给开发机 `localhost:<port>`,流式回传;App PreviewTab 在 relay 模式下载入该隧道 URL。

**Architecture:** ngrok 式反向隧道。① wire 定义隧道帧(tunnel-request/response/chunk/end)。② daemon `proxyToLocalhost`:收 tunnel-request → fetch `127.0.0.1:<port><path>` → 发回 response+chunk(base64)+end 帧(可注入 fetch,真实 HTTP server 单测)。③ relay `TunnelHub`:http req ↔ tunnelId ↔ ServerResponse 关联,把回来的帧写进 res(mock res 单测)。④ 接线:relay httpServer 处理 `/t/<machineId>/<port>/*`、relay WS 路由隧道回帧到 Hub、daemon handleRelayMessage 加 tunnel-request case。⑤ App PreviewTab relay 模式构造隧道 URL。**已知限制(本计划接受)**:路径式隧道对"绝对路径子资源(如 vite 的 /src/x)"会失效;相对路径页面/静态产物可用。完整 SPA 需子域名/URL 重写(后续)。

**Tech Stack:** TypeScript、Node http + 全局 fetch(Node 20)、Zod(@pocket-code/wire)、vitest、react-native-webview。

---

## 背景事实（执行者必读）

- daemon 通过 `connection.send(obj)`(connection.ts 的 RelayConnection)向 relay 发消息;`handleRelayMessage(msg)`(daemon/src/index.ts:154) 的 switch 处理 relay 来的消息,需加 `tunnel-request` case。
- relay 的 `wss.on("connection")`(relay/src/index.ts:69) 处理 daemon/app 消息;现有 `forward-response`/`forward-stream` case(148/160)把 daemon 回包路由给 app。隧道回帧(tunnel-response/chunk/end,来自 daemon,role==="daemon")需路由到 TunnelHub。
- relay 的 httpServer(relay/src/index.ts:27)目前只处理 `/health`;需加 `/t/<machineId>/<port>/*` 路由。
- relay.ts 有 daemon 注册表;需要一个"按 machineId 拿 daemon socket 或直接发原始对象"的能力——若 relay.ts 无现成导出,加一个 `sendRawToDaemon(machineId, obj): boolean`(参考现有 forwardToDaemon 的 daemon 查找)。
- Node 20 有全局 `fetch` 与 `ReadableStream`;`Buffer.from(x,'base64')`/`.toString('base64')` 用于二进制帧。
- 隧道帧在 daemon↔relay WS 上**独立流动**(不包在 forward-request 信封里),relay 直接发给 daemon socket、daemon 直接 connection.send 回。
- server 各包测试经 `vitest run src` 递归扫描。

## 文件结构（本计划涉及）

- 新建：`packages/wire/src/tunnel.ts` + `tunnel.test.ts`;修改 `packages/wire/src/index.ts`(导出)
- 新建：`packages/daemon/src/tunnel.ts` + `tunnel.test.ts`
- 新建：`packages/relay/src/tunnelHub.ts` + `tunnelHub.test.ts`
- 修改：`packages/relay/src/index.ts`(httpServer 路由 + WS 隧道回帧路由)、`packages/relay/src/relay.ts`(加 sendRawToDaemon)
- 修改：`packages/daemon/src/index.ts`(handleRelayMessage 加 tunnel-request case)
- 修改：`packages/app/src/components/PreviewTab/index.tsx`(relay 模式隧道 URL) —— 由实现者读该文件按其现有 URL 输入机制接入

> 所有命令在仓库根目录 `/Users/worsher/code/self/pocket-code` 执行。

---

### Task 1: wire 隧道帧定义

**Files:**
- Create: `packages/wire/src/tunnel.ts`
- Create: `packages/wire/src/tunnel.test.ts`
- Modify: `packages/wire/src/index.ts`

- [ ] **Step 1: 创建隧道帧 schema**

创建 `packages/wire/src/tunnel.ts`，内容：

```typescript
// ── HTTP 隧道帧(relay ↔ daemon) ────────────────────────────
// relay 反向 HTTP 代理:把手机对 /t/<machineId>/<port>/* 的请求经 daemon
// 隧道转给开发机 localhost:<port>,流式回传。帧在 daemon↔relay WS 上独立流动。

import { z } from "zod";

export const TunnelRequest = z.object({
  type: z.literal("tunnel-request"),
  tunnelId: z.string().min(1).max(64),
  port: z.number().int().min(1).max(65535),
  method: z.string().min(1).max(16),
  path: z.string().max(8192),
  headers: z.record(z.string()),
  /** 请求体(base64),GET/HEAD 无。 */
  body: z.string().optional(),
});

export const TunnelResponse = z.object({
  type: z.literal("tunnel-response"),
  tunnelId: z.string(),
  status: z.number().int(),
  headers: z.record(z.string()),
});

export const TunnelChunk = z.object({
  type: z.literal("tunnel-chunk"),
  tunnelId: z.string(),
  /** 响应体分片(base64)。 */
  data: z.string(),
});

export const TunnelEnd = z.object({
  type: z.literal("tunnel-end"),
  tunnelId: z.string(),
  error: z.string().optional(),
});

export const TunnelFrame = z.discriminatedUnion("type", [
  TunnelRequest,
  TunnelResponse,
  TunnelChunk,
  TunnelEnd,
]);

export type TunnelRequestType = z.infer<typeof TunnelRequest>;
export type TunnelResponseType = z.infer<typeof TunnelResponse>;
export type TunnelChunkType = z.infer<typeof TunnelChunk>;
export type TunnelEndType = z.infer<typeof TunnelEnd>;
export type TunnelFrameType = z.infer<typeof TunnelFrame>;
```

- [ ] **Step 2: 从 index 导出**

在 `packages/wire/src/index.ts` 末尾追加：

```typescript

// HTTP 隧道帧(relay ↔ daemon)
export {
  TunnelRequest,
  TunnelResponse,
  TunnelChunk,
  TunnelEnd,
  TunnelFrame,
  type TunnelRequestType,
  type TunnelResponseType,
  type TunnelChunkType,
  type TunnelEndType,
  type TunnelFrameType,
} from "./tunnel.js";
```

- [ ] **Step 3: 创建 schema 测试**

创建 `packages/wire/src/tunnel.test.ts`，内容：

```typescript
import { describe, it, expect } from "vitest";
import { TunnelFrame } from "./tunnel.js";

describe("wire — TunnelFrame", () => {
  it("accepts tunnel-request", () => {
    const r = TunnelFrame.safeParse({
      type: "tunnel-request",
      tunnelId: "t1",
      port: 3000,
      method: "GET",
      path: "/index.html",
      headers: { accept: "text/html" },
    });
    expect(r.success).toBe(true);
  });

  it("accepts tunnel-response / chunk / end", () => {
    expect(
      TunnelFrame.safeParse({ type: "tunnel-response", tunnelId: "t1", status: 200, headers: {} }).success
    ).toBe(true);
    expect(TunnelFrame.safeParse({ type: "tunnel-chunk", tunnelId: "t1", data: "YWJj" }).success).toBe(true);
    expect(TunnelFrame.safeParse({ type: "tunnel-end", tunnelId: "t1" }).success).toBe(true);
    expect(TunnelFrame.safeParse({ type: "tunnel-end", tunnelId: "t1", error: "boom" }).success).toBe(true);
  });

  it("rejects tunnel-request with invalid port", () => {
    expect(
      TunnelFrame.safeParse({ type: "tunnel-request", tunnelId: "t1", port: 0, method: "GET", path: "/", headers: {} }).success
    ).toBe(false);
  });

  it("rejects unknown frame type", () => {
    expect(TunnelFrame.safeParse({ type: "tunnel-foo", tunnelId: "t1" }).success).toBe(false);
  });
});
```

- [ ] **Step 4: 构建 + 测试 + 提交**

Run: `pnpm --filter @pocket-code/wire build && pnpm --filter @pocket-code/wire test`
Expected: 构建成功;wire 测试全过(新增 tunnel.test.ts 4 用例)。

```bash
git add packages/wire/src/tunnel.ts packages/wire/src/tunnel.test.ts packages/wire/src/index.ts
git commit -m "feat(wire): 定义 HTTP 隧道帧(tunnel-request/response/chunk/end)"
```

---

### Task 2: daemon 反向代理 proxyToLocalhost（TDD）

**Files:**
- Create: `packages/daemon/src/tunnel.test.ts`（先写）
- Create: `packages/daemon/src/tunnel.ts`（实现）

- [ ] **Step 1: 先写失败测试（用真实本地 HTTP server）**

创建 `packages/daemon/src/tunnel.test.ts`，内容：

```typescript
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import { createServer, type Server } from "node:http";
import type { AddressInfo } from "node:net";
import { proxyToLocalhost, type TunnelFrame } from "./tunnel.js";

let server: Server;
let port: number;

beforeEach(async () => {
  server = createServer((req, res) => {
    if (req.url === "/hello") {
      res.writeHead(200, { "content-type": "text/plain" });
      res.end("hello tunnel");
    } else if (req.url === "/echo" && req.method === "POST") {
      let body = "";
      req.on("data", (c) => (body += c));
      req.on("end", () => {
        res.writeHead(201, { "content-type": "text/plain" });
        res.end("got:" + body);
      });
    } else {
      res.writeHead(404);
      res.end("nope");
    }
  });
  await new Promise<void>((r) => server.listen(0, "127.0.0.1", r));
  port = (server.address() as AddressInfo).port;
});

afterEach(() => {
  server.close();
});

function collect(frames: TunnelFrame[]) {
  return (f: TunnelFrame) => frames.push(f);
}

describe("proxyToLocalhost", () => {
  it("proxies a GET and emits response + chunk(s) + end", async () => {
    const frames: TunnelFrame[] = [];
    await proxyToLocalhost(
      { tunnelId: "t1", port, method: "GET", path: "/hello", headers: {} },
      collect(frames)
    );
    const resp = frames.find((f) => f.type === "tunnel-response") as any;
    expect(resp.status).toBe(200);
    const body = frames
      .filter((f) => f.type === "tunnel-chunk")
      .map((f: any) => Buffer.from(f.data, "base64").toString("utf-8"))
      .join("");
    expect(body).toBe("hello tunnel");
    expect(frames[frames.length - 1].type).toBe("tunnel-end");
    expect((frames[frames.length - 1] as any).error).toBeUndefined();
  });

  it("forwards a POST body (base64) and gets the echoed response", async () => {
    const frames: TunnelFrame[] = [];
    await proxyToLocalhost(
      {
        tunnelId: "t2",
        port,
        method: "POST",
        path: "/echo",
        headers: { "content-type": "text/plain" },
        body: Buffer.from("ping").toString("base64"),
      },
      collect(frames)
    );
    const resp = frames.find((f) => f.type === "tunnel-response") as any;
    expect(resp.status).toBe(201);
    const body = frames
      .filter((f) => f.type === "tunnel-chunk")
      .map((f: any) => Buffer.from(f.data, "base64").toString("utf-8"))
      .join("");
    expect(body).toBe("got:ping");
  });

  it("emits tunnel-end with error when the upstream is unreachable", async () => {
    const frames: TunnelFrame[] = [];
    // 关掉 server,端口不可达
    server.close();
    await new Promise((r) => setTimeout(r, 50));
    await proxyToLocalhost(
      { tunnelId: "t3", port, method: "GET", path: "/hello", headers: {} },
      collect(frames)
    );
    const end = frames.find((f) => f.type === "tunnel-end") as any;
    expect(end).toBeDefined();
    expect(end.error).toBeTruthy();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/daemon test`
Expected: FAIL —— 无法解析 `./tunnel.js`。

- [ ] **Step 3: 实现 proxyToLocalhost**

创建 `packages/daemon/src/tunnel.ts`，内容：

```typescript
// ── daemon 反向代理(隧道接收端) ─────────────────────────────
// 收 relay 来的 tunnel-request,请求开发机 localhost:<port>,把响应
// 切成 tunnel-response + tunnel-chunk(base64) + tunnel-end 帧回传。

export interface TunnelHttpRequest {
  tunnelId: string;
  port: number;
  method: string;
  path: string;
  headers: Record<string, string>;
  /** base64 请求体(GET/HEAD 无)。 */
  body?: string;
}

export type TunnelFrame =
  | { type: "tunnel-response"; tunnelId: string; status: number; headers: Record<string, string> }
  | { type: "tunnel-chunk"; tunnelId: string; data: string }
  | { type: "tunnel-end"; tunnelId: string; error?: string };

export async function proxyToLocalhost(
  req: TunnelHttpRequest,
  emit: (frame: TunnelFrame) => void,
  fetchImpl: typeof fetch = fetch
): Promise<void> {
  const url = `http://127.0.0.1:${req.port}${req.path}`;
  try {
    const init: RequestInit = { method: req.method, headers: req.headers };
    if (req.body !== undefined && req.method !== "GET" && req.method !== "HEAD") {
      init.body = Buffer.from(req.body, "base64");
    }
    const resp = await fetchImpl(url, init);

    const headers: Record<string, string> = {};
    resp.headers.forEach((v, k) => {
      headers[k] = v;
    });
    emit({ type: "tunnel-response", tunnelId: req.tunnelId, status: resp.status, headers });

    if (resp.body) {
      const reader = resp.body.getReader();
      for (;;) {
        const { done, value } = await reader.read();
        if (done) break;
        if (value && value.length) {
          emit({
            type: "tunnel-chunk",
            tunnelId: req.tunnelId,
            data: Buffer.from(value).toString("base64"),
          });
        }
      }
    }
    emit({ type: "tunnel-end", tunnelId: req.tunnelId });
  } catch (err: any) {
    emit({ type: "tunnel-end", tunnelId: req.tunnelId, error: err?.message ?? "tunnel proxy failed" });
  }
}
```

- [ ] **Step 4: 跑测试确认全绿 + 提交**

Run: `pnpm --filter @pocket-code/daemon test`
Expected: PASS —— tunnel.test.ts 3 用例全过。

```bash
git add packages/daemon/src/tunnel.ts packages/daemon/src/tunnel.test.ts
git commit -m "feat(daemon): 隧道反向代理 proxyToLocalhost(localhost→帧流)"
```

---

### Task 3: relay TunnelHub（TDD）

**Files:**
- Create: `packages/relay/src/tunnelHub.test.ts`（先写）
- Create: `packages/relay/src/tunnelHub.ts`（实现）

- [ ] **Step 1: 先写失败测试（mock ServerResponse）**

创建 `packages/relay/src/tunnelHub.test.ts`，内容：

```typescript
import { describe, it, expect, vi } from "vitest";
import { TunnelHub } from "./tunnelHub.js";

function mockRes() {
  return {
    writeHead: vi.fn(),
    write: vi.fn(),
    end: vi.fn(),
  } as any;
}

describe("TunnelHub", () => {
  it("writes response head, chunks, and ends to the registered res", () => {
    const hub = new TunnelHub();
    const res = mockRes();
    hub.open("t1", res);
    hub.onResponse("t1", 200, { "content-type": "text/html", "transfer-encoding": "chunked" });
    hub.onChunk("t1", Buffer.from("hi").toString("base64"));
    hub.onEnd("t1");

    // 状态码写入,且过滤了 transfer-encoding 这类逐跳头
    expect(res.writeHead).toHaveBeenCalledTimes(1);
    const [status, headers] = res.writeHead.mock.calls[0];
    expect(status).toBe(200);
    expect(headers["content-type"]).toBe("text/html");
    expect(headers["transfer-encoding"]).toBeUndefined();
    expect(Buffer.from(res.write.mock.calls[0][0]).toString("utf-8")).toBe("hi");
    expect(res.end).toHaveBeenCalled();
    expect(hub.size).toBe(0); // end 后清理
  });

  it("ignores frames for unknown tunnelId", () => {
    const hub = new TunnelHub();
    expect(() => hub.onChunk("nope", "YWJj")).not.toThrow();
    expect(() => hub.onEnd("nope")).not.toThrow();
  });

  it("on error end without prior response writes a 502", () => {
    const hub = new TunnelHub();
    const res = mockRes();
    hub.open("t2", res);
    hub.onEnd("t2", "upstream down");
    expect(res.writeHead).toHaveBeenCalledWith(502);
    expect(res.end).toHaveBeenCalled();
  });

  it("abortAll ends all pending tunnels", () => {
    const hub = new TunnelHub();
    const r1 = mockRes();
    const r2 = mockRes();
    hub.open("a", r1);
    hub.open("b", r2);
    hub.abortAll();
    expect(r1.end).toHaveBeenCalled();
    expect(r2.end).toHaveBeenCalled();
    expect(hub.size).toBe(0);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @pocket-code/relay test`
Expected: FAIL —— 无法解析 `./tunnelHub.js`。

- [ ] **Step 3: 实现 TunnelHub**

创建 `packages/relay/src/tunnelHub.ts`，内容：

```typescript
// ── 隧道枢纽(relay 侧) ──────────────────────────────────────
// 关联 http 请求(ServerResponse)与 tunnelId:把 daemon 回来的
// tunnel-response/chunk/end 帧写进对应的 HTTP 响应。

import type { ServerResponse } from "http";

interface PendingTunnel {
  res: ServerResponse;
  responded: boolean;
}

// 逐跳头:不应原样透传(我们用 write/end 隐式 chunked)。
const HOP_BY_HOP = new Set(["transfer-encoding", "connection", "content-length", "keep-alive"]);

export class TunnelHub {
  private tunnels = new Map<string, PendingTunnel>();

  open(tunnelId: string, res: ServerResponse): void {
    this.tunnels.set(tunnelId, { res, responded: false });
  }

  onResponse(tunnelId: string, status: number, headers: Record<string, string>): void {
    const t = this.tunnels.get(tunnelId);
    if (!t || t.responded) return;
    t.responded = true;
    const safe: Record<string, string> = {};
    for (const [k, v] of Object.entries(headers)) {
      if (!HOP_BY_HOP.has(k.toLowerCase())) safe[k] = v;
    }
    try {
      t.res.writeHead(status, safe);
    } catch {
      /* res 已结束/出错,忽略 */
    }
  }

  onChunk(tunnelId: string, dataBase64: string): void {
    const t = this.tunnels.get(tunnelId);
    if (!t) return;
    try {
      t.res.write(Buffer.from(dataBase64, "base64"));
    } catch {
      /* ignore */
    }
  }

  onEnd(tunnelId: string, error?: string): void {
    const t = this.tunnels.get(tunnelId);
    if (!t) return;
    this.tunnels.delete(tunnelId);
    try {
      if (!t.responded && error) {
        t.res.writeHead(502);
      }
      t.res.end();
    } catch {
      /* ignore */
    }
  }

  /** 中止所有挂起隧道(如 daemon 断线时)。 */
  abortAll(error = "tunnel closed"): void {
    for (const [id] of this.tunnels) this.onEnd(id, error);
  }

  get size(): number {
    return this.tunnels.size;
  }
}
```

- [ ] **Step 4: 跑测试确认全绿 + 提交**

Run: `pnpm --filter @pocket-code/relay test`
Expected: PASS —— tunnelHub.test.ts 4 用例全过。

```bash
git add packages/relay/src/tunnelHub.ts packages/relay/src/tunnelHub.test.ts
git commit -m "feat(relay): TunnelHub(http 请求↔隧道帧关联)"
```

---

### Task 4: 接线 relay httpServer/WS + daemon

**Files:**
- Modify: `packages/relay/src/relay.ts`（加 sendRawToDaemon）
- Modify: `packages/relay/src/index.ts`（httpServer `/t/*` 路由 + WS 隧道回帧路由 + Hub 实例）
- Modify: `packages/daemon/src/index.ts`（handleRelayMessage 加 tunnel-request case）

- [ ] **Step 1: relay.ts 增加按 machineId 发原始对象**

读 `packages/relay/src/relay.ts`,参照 `forwardToDaemon` 里"按 machineId 查 daemon socket"的逻辑,新增导出:

```typescript
/** 按 machineId 直接给 daemon 发一个原始对象(用于隧道帧)。返回是否找到在线 daemon。 */
export function sendRawToDaemon(machineId: string, obj: unknown): boolean {
  // 复用现有 daemon 注册表查找(与 forwardToDaemon 同源);找到则 ws.send(JSON.stringify(obj))。
  // 具体实现按 relay.ts 现有 daemons 数据结构填充。
}
```
（实现者按 relay.ts 现有 daemon 注册表结构补全函数体;forwardToDaemon 已有相同查找,照搬其 socket 获取与 readyState 检查。）

- [ ] **Step 2: relay/index.ts 接入 httpServer 路由 + Hub + WS 回帧**

在 `packages/relay/src/index.ts`:
1. 顶部 import: `import { TunnelHub } from "./tunnelHub.js";` 和 `import { ... sendRawToDaemon } from "./relay.js";`(并入现有 relay.js import),`import crypto from "crypto"` 已存在。
2. 模块作用域加: `const tunnelHub = new TunnelHub();`
3. httpServer 回调(现 `/health` 之后、404 之前)加 `/t/<machineId>/<port>/*` 路由:

```typescript
    // ── 反向 HTTP 隧道: /t/<machineId>/<port>/<rest> ──
    const tunnelMatch = url.pathname.match(/^\/t\/([^/]+)\/(\d+)(\/.*)?$/);
    if (tunnelMatch) {
      const [, machineId, portStr, rest] = tunnelMatch;
      const port = parseInt(portStr, 10);
      const tunnelId = crypto.randomUUID();
      const headers: Record<string, string> = {};
      for (const [k, v] of Object.entries(req.headers)) {
        if (typeof v === "string") headers[k] = v;
        else if (Array.isArray(v)) headers[k] = v.join(", ");
      }
      const chunks: Buffer[] = [];
      req.on("data", (c) => chunks.push(c as Buffer));
      req.on("end", () => {
        const body = chunks.length ? Buffer.concat(chunks).toString("base64") : undefined;
        tunnelHub.open(tunnelId, res);
        const ok = sendRawToDaemon(machineId, {
          type: "tunnel-request",
          tunnelId,
          port,
          method: req.method || "GET",
          path: (rest || "/") + (url.search || ""),
          headers,
          body,
        });
        if (!ok) tunnelHub.onEnd(tunnelId, `daemon ${machineId} offline`);
      });
      return;
    }
```

4. 在 WS 消息 switch 里(daemon 来的消息,与 forward-response 同级)加隧道回帧路由:

```typescript
        case "tunnel-response": {
          if (role !== "daemon" || !msg.tunnelId) return;
          tunnelHub.onResponse(msg.tunnelId, msg.status, msg.headers || {});
          break;
        }
        case "tunnel-chunk": {
          if (role !== "daemon" || !msg.tunnelId) return;
          tunnelHub.onChunk(msg.tunnelId, msg.data);
          break;
        }
        case "tunnel-end": {
          if (role !== "daemon" || !msg.tunnelId) return;
          tunnelHub.onEnd(msg.tunnelId, msg.error);
          break;
        }
```

5. (健壮性)daemon 断开时中止其隧道:在 `ws.on("close")` 的 `role === "daemon"` 分支里,`unregisterDaemon(ws)` 之后可调 `tunnelHub.abortAll()`(简单起见全部中止;精确按 machineId 关联后续优化)。

- [ ] **Step 3: daemon handleRelayMessage 加 tunnel-request**

在 `packages/daemon/src/index.ts`:
1. 顶部 import: `import { proxyToLocalhost } from "./tunnel.js";`
2. 在 `handleRelayMessage` 的 switch 里(`forward-request` case 之后)加:

```typescript
    // ── 反向 HTTP 隧道请求 ───────────────────────
    case "tunnel-request": {
      proxyToLocalhost(
        {
          tunnelId: msg.tunnelId,
          port: msg.port,
          method: msg.method,
          path: msg.path,
          headers: msg.headers || {},
          body: msg.body,
        },
        (frame) => connection.send(frame)
      ).catch((err: any) => {
        connection.send({ type: "tunnel-end", tunnelId: msg.tunnelId, error: err?.message ?? "tunnel error" });
      });
      break;
    }
```

- [ ] **Step 4: 全链路验证 + 提交**

Run: `pnpm build && pnpm test:all && pnpm typecheck:app && echo ALL GREEN`
Expected: 末尾 `ALL GREEN`(wire/daemon/relay 含新隧道测试全过;daemon、relay、wire 构建通过,证明接线类型正确)。

```bash
git add packages/relay/src/relay.ts packages/relay/src/index.ts packages/daemon/src/index.ts
git commit -m "feat(relay,daemon): 接线反向 HTTP 隧道(/t/<machineId>/<port>/* → localhost)"
```

---

### Task 5: App PreviewTab relay 隧道 URL

**Files:**
- Modify: `packages/app/src/components/PreviewTab/index.tsx`

- [ ] **Step 1: 读 PreviewTab 现有 URL 输入机制并接入隧道 URL helper**

读 `packages/app/src/components/PreviewTab/index.tsx`,理解其现有的 URL 状态/输入栏。新增一个辅助:当处于 relay 模式(settings.workspaceMode === "relay" 且有 relayServerUrl + relayMachineId)时,提供"在中继预览端口"的入口:给定端口 N,构造 URL:

```typescript
// relayServerUrl 形如 ws://host:3200 → http://host:3200/t/<machineId>/<N>/
function buildTunnelUrl(relayServerUrl: string, machineId: string, port: number): string {
  const httpBase = relayServerUrl.replace(/^ws:\/\//, "http://").replace(/^wss:\/\//, "https://");
  return `${httpBase.replace(/\/$/, "")}/t/${machineId}/${port}/`;
}
```

接入方式由实现者按 PreviewTab 现有结构决定(最简:URL 输入栏旁加一个"端口→隧道"快捷,或在 relay 模式下把用户输入的 `localhost:N` / `N` 自动改写为隧道 URL)。PreviewTab 需要 settings(含 workspaceMode/relayServerUrl/relayMachineId) —— 若未作为 prop 传入,由实现者从 App.tsx 传入或经 context 获取。保持改动聚焦,不改动 LAN/本地模式现有行为。

- [ ] **Step 2: 验证编译 + 提交**

Run: `pnpm typecheck:app`
Expected: 0 错误。

```bash
git add packages/app/src/components/PreviewTab/index.tsx packages/app/App.tsx
git commit -m "feat(app): PreviewTab relay 模式构造中继隧道预览 URL"
```

---

## Self-Review

**1. Spec coverage（对照 spec 第 5.2 节中继 HTTP 隧道）：**
- ✅ relay 反向 HTTP 代理 `/t/<machineId>/<port>/*` → daemon → localhost → 流式回传 → Task 2/3/4
- ✅ 隧道帧协议在 wire → Task 1
- ✅ App 构造隧道 URL → Task 5
- 注:HMR(WS upgrade 隧道)= 后续;绝对路径子资源限制已在 Architecture 说明;daemon 断线精确按 machineId 关联中止 = 后续优化(当前 abortAll)。

**2. Placeholder scan：** Task 1-3 含完整代码;Task 4-5 的"实现者按现有结构补全"是对 relay.ts daemon 注册表与 PreviewTab 现有 URL 机制的必要适配点(给了精确的新增函数签名/路由代码/接入位置),非占位空话。

**3. Type consistency：** daemon `proxyToLocalhost` 产出的 TunnelFrame(response/chunk/end)字段与 relay TunnelHub 的 onResponse/onChunk/onEnd 入参一致;与 wire 的 TunnelFrame schema 字段一致(tunnelId/status/headers/data/error);relay `/t/` 路由发出的 tunnel-request 字段(tunnelId/port/method/path/headers/body)与 daemon tunnel-request case 读取一致。

**4. 风险：** ① 全局 fetch 流式读取在 Node 20 可用(daemon 运行于 Node);② base64 分片对大响应有开销,MVP 可接受;③ 真机完整 E2E 需 relay+daemon+dev server+App 四组件,本计划以单测(Task1-3)+构建/类型(Task4-5)验证逻辑与接线,四组件 E2E 作为验收(后续可如 P3b/P4-part3 那样自驱)。
